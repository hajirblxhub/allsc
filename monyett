-- MEMEK LO
local RunService = game:GetService("RunService")
local cloneref = (cloneref or clonereference or function(instance) return instance end)
local ReplicatedStorage = cloneref(game:GetService("ReplicatedStorage"))

-- Noxius UI (ganti WindUI)
local Noxius
do
    local ok, result = pcall(function()
        return loadstring(readfile("lib/lib.lua"))()
    end)
    if ok and result and result.Window then
        Noxius = result
    else
        Noxius = loadstring(game:HttpGet("https://raw.githubusercontent.com/vorahubjaya-design/ssst/refs/heads/main/lib.lua"))()
    end
end

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local isMobile = game:GetService("UserInputService").TouchEnabled
local net = ReplicatedStorage:WaitForChild("Packages", 10):WaitForChild("_Index", 10):WaitForChild("sleitnick_net@0.2.0",
    10):WaitForChild("net", 10)
local remotes = net:GetChildren()
function GetServerRemote(targetName)
    local allRemotes = net:GetChildren()
    for i, remote in ipairs(allRemotes) do
        if remote.Name == targetName then
            -- Mengambil urutan berikutnya (Index + 1)
            local actualRemote = allRemotes[i + 1]
            return actualRemote
        end
    end
    return nil
end

function GetServerRemoteReverse(targetName)
    local allRemotes = net:GetChildren()
    for i, remote in ipairs(allRemotes) do
        if remote.Name == targetName then
            -- Mengambil urutan berikutnya (Index + 1)
            local actualRemote = allRemotes[i - 1]
            return actualRemote
        end
    end
    return nil
end

print("remotes length: " .. #remotes)

local Connections = {}
local Events = {}
local mutationTarget = {}
local Tasks = {}
local lastTimeFishCaught
local magix = -2

if #remotes ~= 323 then
    magix = 0
end

_G.SavedData = _G.SavedData or {
    FishCaught = {},
    CaughtVisual = {},
    FishNotif = {}
}
function FireLocalEvent(remote, ...)
    local args = {...}
    local signal = remote.OnClientEvent
    -- Mencari koneksi lokal yang sedang mendengarkan remote ini
    for _, connection in pairs(getconnections(signal)) do
        if connection.Function then
            task.spawn(function()
                connection.Function(unpack(args))
            end)
        end
    end
end
function printTable(tbl, indent)
    indent = indent or 0
    local spacing = string.rep("  ", indent)

    for key, value in pairs(tbl) do
        if type(value) == "table" then
            print(spacing .. tostring(key) .. " = {")
            printTable(value, indent + 1)
            print(spacing .. "}")
        else
            print(spacing .. tostring(key) .. " = " .. tostring(value))
        end
    end
end
local saveCount = 0
function HookRemote(humanName, storageKey)
    local remote = GetServerRemote(humanName)
    if remote then
        remote.OnClientEvent:Connect(function(...)
          if saveCount < 7 then
            _G.SavedData[storageKey] = {...}
            --print("ðŸ“¥ Data captured for: " .. humanName)
            --printTable({...})
            local args = {...}
            if storageKey == "CaughtVisual" and tostring(args[1]) == tostring(LocalPlayer.Name)  then
                saveCount = saveCount + 1
                -- print(LocalPlayer.Name)
               
            end
            
          end
          -- saved = true
        end)
        return true
    end
    return false
end

print(len)


local Controllers
local NotificationController
local GiftingController
local TextNotificationController
local VFXController
local CutsceneController
local ControlModule
local FishingController
local AFKController
local PromptController
local BackpackController
local Replion
local PlayerData
local ItemUtility
local TierUtility

if isMobile then
    Controllers = ReplicatedStorage:WaitForChild("Controllers")
    NotificationController = require(Controllers:WaitForChild("NotificationController"))
    GiftingController = require(Controllers:WaitForChild("GiftingController"))
    TextNotificationController = require(Controllers:WaitForChild("TextNotificationController"))
    VFXController = require(Controllers:WaitForChild("VFXController"))
    FishingController = require(Controllers:WaitForChild("FishingController"))
    AFKController = require(Controllers:WaitForChild("AFKController"))
    PromptController = require(Controllers:WaitForChild("PromptController"))
    BackpackController = require(Controllers:WaitForChild("BackpackController"))
    CutsceneController = require(Controllers:WaitForChild("CutsceneController"))
    Replion = require(ReplicatedStorage.Packages.Replion)
    PlayerData = Replion.Client:WaitReplion("Data")
    ControlModule = require(LocalPlayer:WaitForChild("PlayerScripts"):WaitForChild("PlayerModule"):WaitForChild("ControlModule"))
    Promise = require(ReplicatedStorage:WaitForChild("Packages").Promise)
    ItemUtility = require(ReplicatedStorage.Shared.ItemUtility)
    TierUtility = require(ReplicatedStorage:WaitForChild("Shared"):WaitForChild("TierUtility"))
    print("mobile")
else
    print("ðŸ’» Player menggunakan PC / Laptop")
    Controllers = ReplicatedStorage:WaitForChild("Controllers")
    BackpackController = require(Controllers:WaitForChild("BackpackController"))
    Replion = require(ReplicatedStorage.Packages.Replion)
    PlayerData = Replion.Client:WaitReplion("Data")
end

local needCast = true

local st = {
    autoFavEnabled = false,
    autoFavVariantEnabled = false,
}

local Config = {
    MainLoop = true,
    InstantFishing = false,
    AutoCatch = false,
    Perfection = false,
    isFarming = false,
    isMinig = false,
    AutoFarm = false,
    AutoFarmMegalodon = false,
    AutoFarmChristmas = false,
    AutoFavorite = false,
    CatchDelay = 0.7,
    CastDelay = 1.12,
    ChargeDelay = 0,
    CancelDelay = 0.78,
    DisableAnimations = false,
    PerfectionValue = 0.28,
    onlyMythicAndSecret = false,
    onlyRareAndEpic = false,
    onlySecret = false,
    autoWeather = false,
    removePopUp = false,
    antiFall = false,
    gpuSaver = false,
    customEffect = false,
    AutoSell = true,
    SellDelay = 15,
    SpeedHack = false,
    SpeedHackValue = 60,
    WalkOnWater = false,
    HookNotif = false,
    BackupPerfection = false,
    AutoTradeEvolveEnchant = false,
    AutoAcceptTrade = false,
    AutoSendTrade = false,
    FavRubyGemstone = false,
    isFreeze = false,
    backupIsFreeze = false,
    autoFishing = false,
    DisablePopUp = false,
    DisableVfx = false,
    DisableCutscene = false,
    CustomWebhook = false,
    WebhookUrl = "https://discord.com/api/webhooks/1461727812917526681/PY7QeWuw4dHp8NmurEcHiLMJIx06Akxwpna_wpWXA1QKayarcy4W73k25gtrpHQ77pq5",
    CustomWebhookUrl = "",
    HideUsn = false,
    AutoMining = false,
    axeUuid = "",
    autoConsumeCaveCrystal = false,
    savePosition = false,
    position = "",
    antiOKOK = false,
    SpinPlayer = false,
    UB = {
        Active = false,
        Settings = {
            CompleteDelay = 3.7,
            CancelDelay = 0.25
        },
        Remotes = {},
        Stats = {
            castCount = 0,
            startTime = 0
        }
    }, 
    amblatant = false,
}

local needCast, skip = false, false

local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local RunService = game:GetService("RunService")

local LocalPlayer = Players.LocalPlayer

local IntervalWaktu = 60 * 18

for i, v in pairs(getconnections(LocalPlayer.Idled)) do
    if v.Disable then v:Disable() elseif v.Disconnect then v:Disconnect() end
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "TalonBypass_Ghost"
ScreenGui.Parent = CoreGui

local MainFrame = Instance.new("Frame")
MainFrame.Parent = ScreenGui
MainFrame.BackgroundTransparency = 1 
MainFrame.Position = UDim2.new(0.5, -206, 0.5, -35)
MainFrame.Size = UDim2.new(0, 80, 0, 30)
MainFrame.Active = true
MainFrame.Draggable = false 

local UICorner = Instance.new("UICorner", MainFrame)
UICorner.CornerRadius = UDim.new(0, 8)

local Label = Instance.new("TextLabel")
Label.Parent = MainFrame
Label.Size = UDim2.new(1, 0, 1, 0)
Label.BackgroundTransparency = 1
Label.Text = ""
Label.TextColor3 = Color3.fromRGB(255, 255, 255)
Label.TextSize = 10
Label.Font = Enum.Font.GothamBold
Label.TextWrapped = true

function FireUIAtLocation()
    local x = MainFrame.AbsolutePosition.X + (MainFrame.AbsoluteSize.X / 2)
    local y = MainFrame.AbsolutePosition.Y + (MainFrame.AbsoluteSize.Y / 2)
    
    local guiObjects = LocalPlayer.PlayerGui:GetGuiObjectsAtPosition(x, y)
    
    for _, obj in pairs(guiObjects) do
        if obj:IsA("GuiButton") then
            for _, connection in pairs(getconnections(obj.MouseButton1Click)) do
                connection:Fire()
            end
            for _, connection in pairs(getconnections(obj.Activated)) do
                connection:Fire()
            end
            
            obj.BackgroundTransparency = 0.8
            task.delay(0.1, function() obj.BackgroundTransparency = 0 end)
        end
    end
end

task.spawn(function()
    while true do
        task.wait(IntervalWaktu)
        FireUIAtLocation()
        print("Talon Bypass Triggered.")
    end
end)

RunService.Heartbeat:Connect(function()
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        LocalPlayer.Character.HumanoidRootPart.CFrame = LocalPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0.00001, 0)
    end
end)

function safeFire(func)
    task.spawn(function()
        pcall(func)
    end)
end

function safeCall(func)
    pcall(func)
end

local origDeliverNotification = isMobile and TextNotificationController.DeliverNotification

function hookNotification()
    if isMobile then
        TextNotificationController.DeliverNotification = function(self, data, ...)
            if Config.autoFishing then
                print(tostring(data.Text))
                if string.find(tostring(data.Text), "stopped") then
                    print("stopped")
                    Events.UpdateAutoFishingState:InvokeServer(true)
                end
            end
            return origDeliverNotification(self, data, ...)
        end
    end
end

hookNotification()

Events.equip = GetServerRemote("RF/EquipToolFromHotbar") --remotes[95 + magix]
Events.unequip = GetServerRemote("RE/UnequipToolFromHotbar")
Events.equipItem = GetServerRemote("RE/EquipItem")
Events.CancelFishingInputs = GetServerRemote("RF/CancelFishingInputs")
Events.BuyWeather = GetServerRemote("RF/PurchaseWeatherEvent")

function getAxeUUID()
    Config.axeUuid = ""
    if not PlayerData then return end
    local inv = PlayerData:GetExpect("Inventory")
    if not inv or not inv.Items then return end
    for _, item in pairs(inv.Items) do
        if item and item.Id == 20220 then
            Config.axeUuid = item.UUID or ""
            return
        end
    end
end

function pickAxe()
    getAxeUUID()
    if not Config.axeUuid or Config.axeUuid == "" then return end
    if not BackpackController or not BackpackController.ToggleHotbarItem then return end
    print("pick axe")
    Events.CancelFishingInputs:InvokeServer()
    task.wait(0.7)
    Events.unequip:FireServer()
    print("pick axe unequip")
    task.wait(1)
    BackpackController.ToggleHotbarItem(self, Config.axeUuid)
    print(Config.axeUuid)
end

local isNoFallActive = false
local platform = nil

function createPlatform(position)
    if platform then return end
    platform = Instance.new("Part")
    platform.Name = "AirPlatform"
    platform.Size = Vector3.new(7, 1, 7)
    platform.Anchored = true
    platform.CanCollide = true
    platform.Transparency = 0.9
    platform.Position = position - Vector3.new(0, 3, 0)
    platform.Parent = workspace
end

function equipRod()
    Events.equip:InvokeServer(1)

    if Config.autoFishing or Config.AutoCatch then
        -- DisableController(FishingController)
        Events.UpdateAutoFishingState:InvokeServer(true)
    end
end

local blatantFishCycleCount = 0
local rainbow = 0
local golden = 0
local inventoryFish = 0

function ub_loop()
    while Config.UB.Active do
        -- print("ub_loop")
        if Config.isMinig then
            task.wait(3)
        else
            local currentTime = tick()

            -- Cancel dengan CancelDelay
            safeFire(function()
                Config.UB.Remotes.CancelFishingInputs:InvokeServer()
                
            end)

            if Config.autoFishing then Events.UpdateAutoFishingState:InvokeServer(true) end

            task.wait(needCast and 0.7 or Config.UB.Settings.CancelDelay)
            needCast = false

            -- 1x CHARGE & REQUEST (CASTING)
            safeFire(function()
                --Config.UB.Remotes.equip:InvokeServer(1)
                Config.UB.Remotes.ChargeFishingRod:InvokeServer({ [1] = currentTime })
                if Config.antiOKOK and not Config.autoFishing then
                    local delay = math.random(15, 17) / 100
                    task.wait(delay)
                    -- print(delay)
                end
                --print("casting")
                -- print(Config.antiOKOK)
                Config.UB.Remotes.RequestMinigame:InvokeServer(1, 0, currentTime)
            end)

            task.wait(Config.UB.Settings.CompleteDelay)
            
            -- RARITY FILTER
            if not skip then
                safeFire(function()
                    Config.UB.Remotes.FishingCompleted:InvokeServer()
                    if Config.amblatant and isCaught then
                        task.spawn(function ()
                            task.wait(0.3)
                            local xremote = GetServerRemote("RE/FishCaught")
                            if xremote then
                                FireLocalEvent(xremote, unpack(_G.SavedData.FishCaught))
                            end
                            xremote = GetServerRemote("RE/CaughtFishVisual")
                            if xremote then
                                FireLocalEvent(xremote, unpack(_G.SavedData.CaughtVisual))
                            end
                            xremote = GetServerRemote("RE/ObtainedNewFishNotification")
                            if xremote then
                                FireLocalEvent(xremote, unpack(_G.SavedData.FishNotif))
                            end
                        end)
                        isCaught = false
                    end
                end)
            end
            blatantFishCycleCount = blatantFishCycleCount + 1
        end
    end
end

function UB_init()
    local success, netFolder = pcall(function()
        return ReplicatedStorage:WaitForChild("Packages", 10)
            :WaitForChild("_Index", 10)
            :WaitForChild("sleitnick_net@0.2.0", 10)
            :WaitForChild("net", 10)
    end)
    if not success or not netFolder then
        warn("Gagal menemukan folder jaringan (net). Pastikan script berjalan di game yang benar.")
        return false
    end
    local remotes = netFolder:GetChildren()
    
    Config.UB.Remotes.ChargeFishingRod = GetServerRemote("RF/ChargeFishingRod") -- remotes[127 + magix]
    Config.UB.Remotes.RequestMinigame = GetServerRemote("RF/RequestFishingMinigameStarted") --remotes[135 + magix]
    Config.UB.Remotes.CancelFishingInputs = GetServerRemote("RF/CancelFishingInputs") --remotes[29 + magix]
    Config.UB.Remotes.UpdateAutoFishingState = GetServerRemote("RF/UpdateAutoFishingState") --remotes[121 + magix]
    Config.UB.Remotes.FishingCompleted = GetServerRemote("RF/CatchFishCompleted") --remotes[141 + magix]
    Config.UB.Remotes.MinigameChanged = GetServerRemote("RE/FishingMinigameChanged") --remotes[135 + magix]
    Config.UB.Remotes.equip = GetServerRemote("RF/EquipToolFromHotbar") --remotes[95 + magix]
    return true
end
UB_init()

function UB_start()
    -- Config.UB.Active = false
    if Config.UB.Active then
        print("UB sudah aktif.")
        return
    end

    if not UB_init() then
        print("Gagal menginisialisasi UB. Tidak bisa memulai.")
        return
    end

    Config.UB.Active = true
    needCast = true
    Config.UB.Stats.castCount = 0
    Config.UB.Stats.startTime = tick()
    -- print("Memulai loop Universal Bypass...")
    Config.UB.Settings.CompleteDelay = Config.UB.Settings.CompleteDelay
    Config.UB.Settings.CancelDelay = Config.UB.Settings.CancelDelay
    Tasks.ubtask = task.spawn(ub_loop)
end

function UB_stop()
    if not Config.UB.Active then
        print("UB sudah tidak aktif.")
        return
    end

    Config.UB.Active = false
    -- print("Menghentikan Universal Bypass...")

    -- Cancel fishing inputs
    safeFire(function()
        if Config.UB.Remotes.CancelFishingInputs then
            
            Config.UB.Remotes.CancelFishingInputs:InvokeServer()
        end
    end)
    
    -- Enable game auto fishing
      task.wait(0.2)

    if Tasks.ubtask then
        task.cancel(Tasks.ubtask)
        Tasks.ubtask = nil
    end
    local runtime = tick() - Config.UB.Stats.startTime
    print("UB dihentikan. Waktu berjalan: " .. string.format("%.2f", runtime) .. " detik.")
end

function castRod()
    pcall(function()
        print('cancel')
        Events.CancelFishingInputs:InvokeServer()
        task.wait(0.7)
        Events.charge:InvokeServer()
        Events.minigame:InvokeServer(-911.1024780273438, 0.9, os.clock())
    end)
end

function onToggleUB(value)
    Config.HookNotif = true
    -- hookNotification()
    if value then
        -- Config.UB.Remotes.equip:InvokeServer(1)
        equipRod()
        castRod()
        Config.UB.Settings.CompleteDelay = Config.UB.Settings.CompleteDelay
        Config.UB.Settings.CancelDelay = Config.UB.Settings.CancelDelay
        UB_start()
    else
        UB_stop()
        if Tasks.ubtask then
            task.cancel(Tasks.ubtask)
            Tasks.ubtask = nil
        end
    end
end
UB_init()

local targetRarities = {
    mythic = false,
    legendary = false,
    epic = false,
    rare = false,
    uncommon = false
}

local targetWebhookRarities = {
    mythic = false,
    legendary = false,
    epic = false,
    rare = false,
    uncommon = false
}

local CONFIG_FILE = "config.json"
local MUTATION_CONFIG = "mutation_config.json"
local RARITY_CONFIG = "rarity_config.json"
local RARITY_WH_CONFIG = "rarity_wh_config.json"

function LoadConfig()
    task.spawn(function()
        if isfile(CONFIG_FILE) then
            local ok, data = pcall(function()
                return HttpService:JSONDecode(readfile(CONFIG_FILE))
            end)
            if ok and typeof(data) == "table" then
                for k, v in pairs(data) do
                    if Config[k] ~= nil then
                        Config[k] = v
                        print(k, v)
                    end
                end
            end
            print("xsuccess 1")
        end
    end)

    task.spawn(function()
        if isfile(MUTATION_CONFIG) then
            print("ada config mutasi")
            local ok, data = pcall(function()
                return HttpService:JSONDecode(readfile(MUTATION_CONFIG))
            end)
            if not ok then
                warn("JSONDecode error:", data)
                return false
            end
            if typeof(data) ~= "table" then
                warn("data bukan table:", typeof(data))
                return false
            end
            print("data type:", typeof(data))
            print("data length:", #data)
            table.clear(mutationTarget)
            for i, v in ipairs(data) do
                print("load mutation:", i, v, typeof(v))
                mutationTarget[i] = v
            end
            print("mutationTarget loaded:", #mutationTarget)
            return true
        end
    end)

    task.spawn(function()
        if isfile(RARITY_CONFIG) then
            local ok, data = pcall(function()
                return HttpService:JSONDecode(readfile(RARITY_CONFIG))
            end)
            if ok and typeof(data) == "table" then
                for k, v in pairs(data) do
                    if targetRarities[k] ~= nil then
                        targetRarities[k] = v
                    end
                end
            end
            print("xsuccess 3")
        end
    end)

    task.spawn(function()
        if isfile(RARITY_WH_CONFIG) then
            local ok, data = pcall(function()
                return HttpService:JSONDecode(readfile(RARITY_WH_CONFIG))
            end)
            if ok and typeof(data) == "table" then
                for k, v in pairs(data) do
                    if targetWebhookRarities[k] ~= nil then
                        targetWebhookRarities[k] = v
                    end
                end
            end
            print("xsuccess 4")
        end
    end)
end

function SaveConfig(type)
    if type == 1 then
        writefile(CONFIG_FILE, HttpService:JSONEncode(Config))
    elseif type == 2 then
        writefile(MUTATION_CONFIG, HttpService:JSONEncode(mutationTarget))
    elseif type == 3 then
        writefile(RARITY_CONFIG, HttpService:JSONEncode(targetRarities))
    elseif type == 4 then
        writefile(RARITY_WH_CONFIG, HttpService:JSONEncode(targetWebhookRarities))
    end
end

function SaveAllConfig()
    SaveConfig(1)
    SaveConfig(2)
    SaveConfig(3)
    SaveConfig(4)
end

LoadConfig()

Config.InstantFishing = false
Config.BlatantFishing = false
Config.AutoCatch = false
Config.UB.Active = false

Events.systemMessageEvent = GetServerRemote("RE/DisplaySystemMessage")
Events.fishNotif = GetServerRemote("RE/ObtainedNewFishNotification")

Events.systemMessageEvent.OnClientEvent:Connect(function(...)
    local args = {...}
    local character = LocalPlayer.Character
    if not character then return end
    local rootPart = character:FindFirstChild("HumanoidRootPart")
    if not rootPart then return end
    if args[1] then args[1] = args[1]:lower() end
    
    if args[1] and (not string.find(args[1], "global") or string.find(args[1], "server")) and string.find(args[1], "staff") then
        game.Players.LocalPlayer:Kick("Awas ada sule, eh staff maksudnya")
    end
end)

local LOCATIONS = {
	["Megalodon"] = CFrame.new(-1172.987, 7.924, 3620.589, 0.706693, 0, 0.707521, 0, 1, 0, -0.707521, 0, 0.706693),
    ["Fisherman"] = CFrame.new(-18.065, 9.532, 2734.000, -0.113811, 0.000000, -0.993502, -0.000000, 1.000000, 0.000000, 0.993502, 0.000000, -0.113811),
    ["Sisyphus Statue"] = CFrame.new(-3754.441, -135.074, -895.376, 0.943844, 0, -0.330393, 0, 1, 0, 0.330393, 0, 0.943844),
    ["Coral Reefs"] = CFrame.new(-3030.043, 2.509, 2271.429, 0.304264, -0.000000, 0.952588, -0.000000, 1.000000, 0.000000, -0.952588, -0.000000, 0.304264),
    ["Esoteric Depths"] = CFrame.new(3271.979, -1301.530, 1402.762, -0.981542, -0.000000, -0.191249, -0.000000, 1.000000, 0.000000, 0.191249, 0.000000, -0.981542),
    ["Crater Island 1"] = CFrame.new(990.610, 21.142, 5060.255, 0.998865, 0.000000, -0.047632, -0.000000, 1.000000, -0.000000, 0.047632, 0.000000, 0.998865),
    ["Crater Island 2"] = CFrame.new(1040.036, 55.714, 5131.443, 0.551438, -0.000000, 0.834216, 0.000000, 1.000000, 0.000000, -0.834216, 0.000000, 0.551438),
    ["Lost Isle"] = CFrame.new(-3618.15698, 240.836655, -1317.45801),
    ["Weather Machine"] = CFrame.new(-1488.51196, 83.1732635, 1876.30298),
    ["Tropical Grove"] = CFrame.new(-2132.597, 53.488, 3631.235, -0.664326, -0.000000, 0.747443, -0.000000, 1.000000, 0.000000, -0.747443, -0.000000, -0.664326),
    ["Treasure Room"] = CFrame.new(-3630, -279.074, -1599.287, 0.721647, 0, -0.692261, 0, 1, 0, 0.692261, 0, 0.721647),
    ["Kohana"] = CFrame.new(-663.904236, 3.04580712, 718.796875),
    ["Kohana2"] = CFrame.new(-530.529, 8.750, -72.149, -0.910784, 0, -0.412883, 0, 1, 0, 0.412883, 0, -0.910784),
    ["Underground Cellar"] = CFrame.new(2110.109, -91.199, -699.790, 0.744219, -0.000000, -0.667935, -0.000000, 1.000000, -0.000000, 0.667935, 0.000000, 0.744219),
    ["Ancient Jungle"] = CFrame.new(1837.352, 5.894, -297.224, 0.388620, 0.000000, -0.921398, 0.000000, 1.000000, 0.000000, 0.921398, -0.000000, 0.388620),
    ["Ancient Jungle 2"] = CFrame.new(1468.971, 6.512, -326.397, -0.458676, 0.000000, -0.888603, 0.000000, 1.000000, 0.000000, 0.888603, -0.000000, -0.458676),
    ["Sacred Temple"] = CFrame.new(1459.217, -22.375, -637.787, 0.924266, 0, 0.381750, 0, 1, 0, -0.381750, 0, 0.924266),
    ["Ancient Ruins"] = CFrame.new(6097.176, -585.924, 4644.443, -0.514758, 0, 0.857336, 0, 1, 0, -0.857336, 0, -0.514758),
    ["Pirate Cove"] = CFrame.new(3396.730, 4.192, 3469.213) * CFrame.Angles(-0.000, -1.447, -0.000),
    ["Pirate Treasure Room"] = CFrame.new(3324.07397, -306.475647, 3087.99927, 0.999340534, -1.78439805e-08, 0.0363113917, 2.01013268e-08, 1, -6.18013019e-08, -0.0363113917, 6.24904501e-08, 0.999340534),
    ["Secret Passage"] = CFrame.new(3436.101, -289.845, 3382.640, -0.920254, 0.000000, -0.391321, 0.000000, 1.000000, 0.000000, 0.391321, -0.000000, -0.920254),
    ["Kohana Volcano"] = CFrame.new(-549.192, 20.019, 125.802, 0.955081, 0.000000, -0.296344, -0.000000, 1.000000, 0.000000, 0.296344, -0.000000, 0.955081),
    ["Crystal Depth"] = CFrame.new(5752.219, -907.148, 15343.468, -0.628654, 0.000000, 0.777685, -0.000000, 1.000000, -0.000000, -0.777685, -0.000000, -0.628654),
    ["Lava Basin"] = CFrame.new(950.876, 85.282, -10199.427, 0.105691, -0.000000, 0.994399, -0.000000, 1.000000, 0.000000, -0.994399, -0.000000, 0.105691),
    ["Planetary Observatory"] = CFrame.new(425.919, 3.673, 2187.253), 
    --["Hearthfelt Island"] = CFrame.new(1116.154419, 4.863108, 2721.664551, -0.094682, 0.000000, -0.995508, 0.000000, 1.000000, 0.000000, 0.995508, -0.000000, -0.094682),
    --["Hearthfelt Cave"] = CFrame.new(1319.075562, -53.523628, 2748.127197, -0.850393, 0.000000, 0.526148, -0.000000, 1.000000, -0.000000, -0.526148, -0.000000, -0.85039)
}

function teleport(cf)
    local char = LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart") 
    if not hrp then return end
    hrp.CFrame = cf + Vector3.new(0, 3, 0)
end

function teleportToMegalodon()
    for _, obj in ipairs(workspace:GetDescendants()) do
        if string.find(obj.Name:lower(), "megalodon") then
            task.wait(0.1)
            if obj:IsA("BasePart") then
                teleport(CFrame.new(obj.Position))
                startNoFall()
                atMegalodon = true
                Config.isFarming = true
                local rootpart = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                oldLocation = rootpart.CFrame
                return
            end
        end
    end
end

function teleportTo(locationName)
    local cframe = LOCATIONS[locationName]
    local character = LocalPlayer.Character
    if not character then return end
    local rootPart = character:FindFirstChild("HumanoidRootPart")
    if not rootPart then return end

    if string.find(locationName, "Megalodon") then
        teleportToMegalodon()
    else
        rootPart.CFrame = cframe
        if string.find(locationName, "Ruins") or string.find(locationName, "Christmas") then
            createPlatform(rootPart.Position)
        end
    end
    HookRemote("RE/FishCaught", "FishCaught")
    HookRemote("RE/CaughtFishVisual", "CaughtVisual")
    HookRemote("RE/ObtainedNewFishNotification", "FishNotif")
    currentFishingLocation = cframe
end

getAxeUUID()
pickAxe()

function isExtractPrompt(prompt)
    if not prompt.Enabled then return false end
    if not prompt.ActionText then return false end
    if not prompt.ActionText:lower():find("extract") then return false end

    local parent = prompt.Parent
    if not parent then return false end
    if not parent.Name:lower():find("crystal") then return false end

    return true
end

function fixPrimaryPart(model)
    if model:IsA("Model") and not model.PrimaryPart then
        local part = model:FindFirstChildWhichIsA("BasePart")
        if part then
            model.PrimaryPart = part
        end
    end
end

local TELEPORT_OFFSET = -4

function getHrp()
    local character = LocalPlayer.Character
    local hrp = character:FindFirstChild("HumanoidRootPart")
    return hrp
end

function moveToPrompt(prompt)
    local hrp = getHrp()
    local target = prompt.Parent

    if target:IsA("Model") then
        fixPrimaryPart(target)
        target = target.PrimaryPart
    end

    if target and target:IsA("BasePart") then
        hrp.CFrame = target.CFrame * CFrame.new(0, 0, TELEPORT_OFFSET)
        createPlatform(hrp.Position)
    end
end

function miningCrystal()
    teleportTo("Crystal Depth")
    task.wait(2)
    print("tp to cd")
    task.wait(1.5)
    for _, prompt in ipairs(workspace:GetDescendants()) do
        if prompt:IsA("ProximityPrompt") and isExtractPrompt(prompt) then
            moveToPrompt(prompt)
            task.wait(11)
            fireproximityprompt(prompt)
            task.wait(0.5)
        end
    end
end

function setMining()
    Events.CancelFishingInputs:InvokeServer()
    task.wait(0.7)
    Events.unequip:FireServer()
    task.wait(1)
    pickAxe()
    task.wait(2)
    Config.isFarming = true
    miningCrystal()
end

Events.TextNotification = GetServerRemote("RE/TextNotification")
Events.TextNotification.OnClientEvent:Connect(function(...)
    local args = {...}
    if args[1].Text then
        print("TextNotification text: " .. args[1].Text)
    end
    if args[1].Text and string.find(args[1].Text, "find them") and Config.AutoMining then
        print("waktunya nambang")
        getAxeUUID()
        local rootPart = getRootPart()
        if not rootPart then return end
        local origLocation = rootPart.CFrame
        savedPosition = rootPart.Position
        savedLookVector = rootPart.CFrame.LookVector
        Config.isMinig = true
        toggleFly(true)
        for i = 1, 2 do
            setMining()
            task.wait(3)
        end
        toggleFly(false)
        Config.isMinig = false
        Events.CancelFishingInputs:InvokeServer()
        task.wait(0.8)
        equipRod()
        rootPart.CFrame = CFrame.new(savedPosition, savedPosition + savedLookVector)
        createPlatform(rootPart.Position)
        task.wait(1.5)
        equipRod()
        task.wait(3)
    end
end)

function isTargetVariant(variant)
    for _, v in ipairs(mutationTarget) do
        if v == variant then return true end
    end
    return false
end

function getRootPart()
    local character = LocalPlayer.Character
    if not character then return end
    return character:FindFirstChild("HumanoidRootPart")
end

local PlayerDataReplion = nil
function GetPlayerDataReplion()
    if PlayerDataReplion then return PlayerDataReplion end
    local ReplionModule = ReplicatedStorage:WaitForChild("Packages"):WaitForChild("Replion", 10)
    if not ReplionModule then return nil end
    local ReplionClient = require(ReplionModule).Client
    PlayerDataReplion = ReplionClient:WaitReplion("Data", 5)
    return PlayerDataReplion
end

local MerchantReplion = nil
function GetReplions()
    if MerchantReplion then return true end
    local ReplionModule = ReplicatedStorage:WaitForChild("Packages"):WaitForChild("Replion", 10)
    if not ReplionModule then return false end
    local ReplionClient = require(ReplionModule).Client
    MerchantReplion = ReplionClient:WaitReplion("Merchant", 5)
    return MerchantReplion
end

function GetItemMutationString(item)
    if item.Metadata and item.Metadata.Shiny == true then return "Shiny" end
    return item.Metadata and item.Metadata.VariantId or ""
end

function GetFishNameAndRarity(item)
    local name = item.Identifier or "Unknown"
    local rarity = item.Metadata and item.Metadata.Rarity or "COMMON"
    local itemID = item.Id

    local itemData = nil
    if ItemUtility and itemID then
        pcall(function()
            itemData = ItemUtility:GetItemData(itemID)
            if not itemData then
                local numericID = tonumber(item.Id) or tonumber(item.Identifier)
                if numericID then
                    itemData = ItemUtility:GetItemData(numericID)
                end
            end
        end)
    end

    if itemData and itemData.Data and itemData.Data.Name then
        name = itemData.Data.Name
    end

    if item.Metadata and item.Metadata.Rarity then
        rarity = item.Metadata.Rarity
    elseif itemData and itemData.Probability and itemData.Probability.Chance and TierUtility then
        local tierObj
        pcall(function()
            tierObj = TierUtility:GetTierFromRarity(itemData.Probability.Chance)
        end)
        if tierObj and tierObj.Name then
            rarity = tierObj.Name
        end
    end

    return name, rarity
end

local ImageURLCache = {}
function GetRobloxAssetImage(assetId)
    if not assetId or assetId == 0 then return nil end
    if ImageURLCache[assetId] then
        return ImageURLCache[assetId]
    end
    local url = string.format("https://thumbnails.roblox.com/v1/assets?assetIds=%d&size=420x420&format=Png&isCircular=false", assetId)
    local success, response = pcall(game.HttpGet, game, url)
    if success then
        local ok, data = pcall(HttpService.JSONDecode, HttpService, response)
        if ok and data and data.data and data.data[1] and data.data[1].imageUrl then
            local finalUrl = data.data[1].imageUrl
            ImageURLCache[assetId] = finalUrl
            return finalUrl
        end
    end
    return nil
end

function CensorName(name)
    if not name or type(name) ~= "string" or #name < 1 then
        return "N/A"
    end
    if #name <= 3 then
        return name
    end
    local prefix = name:sub(1, 3)
    local censorString = string.rep("*", #name - 3)
    return prefix .. censorString
end

function sendExploitWebhook(url, username, embed_data)
    local payload = {
        username = username,
        embeds = {embed_data}
    }
    local json_data = HttpService:JSONEncode(payload)
    if typeof(request) == "function" then
        local success, response = pcall(function()
            return request({
                Url = url,
                Method = "POST",
                Headers = { ["Content-Type"] = "application/json" },
                Body = json_data
            })
        end)
        if success and (response.StatusCode == 200 or response.StatusCode == 204) then
            print("Webhook sent successfully.")
            return true, "Sent"
        elseif success and response.StatusCode then
            print("Failed to send webhook. Status code:", response.StatusCode)
            return false, "Failed: " .. response.StatusCode
        elseif not success then
            print("Error sending webhook:", response)
            return false, "Error: " .. tostring(response)
        end
    end
    return false, "No Request Func"
end

function getRarityColor(rarity)
    local r = rarity:upper()
    if r == "SECRET" then return 0x00d3a9 end
    if r == "MYTHIC" then return 0xe22b2e end
    if r == "LEGENDARY" then return 0xFF4500 end
    if r == "EPIC" then return 0x8A2BE2 end
    if r == "RARE" then return 0x0000FF end
    if r == "UNCOMMON" then return 0x00FF00 end
    return 0x00BFFF
end

Events.favorite = GetServerRemote("RE/FavoriteItem")

Connections.fishNotifConnection = Events.fishNotif.OnClientEvent:Connect(function(...)
    local args = {...}
    local arg3 = args[3]
    lastTimeFishCaught = os.clock()

    local dummyItem = {Id = args[1], Metadata = args[2]}
    local fishName, fishRarity = GetFishNameAndRarity(dummyItem)
    local fishRarityUpper = fishRarity:upper()

    local isUserFilterMatch = (fishRarityUpper == "SECRET")
    if Config.CustomWebhook then
        if targetWebhookRarities.mythic and fishRarityUpper == "MYTHIC" then isUserFilterMatch = true end
        if targetWebhookRarities.legendary and fishRarityUpper == "LEGENDARY" then isUserFilterMatch = true end
        if targetWebhookRarities.epic and fishRarityUpper == "EPIC" then isUserFilterMatch = true end
        if fishName == "Ruby" and mutationDisplay == "Gemstone" then isUserFilterMatch = true end
        if fishRarityUpper == "MYTHIC" and mutationDisplay == "Crystalized" then isUserFilterMatch = true end
    end

    if typeof(arg3) == "table" and arg3.InventoryItem and arg3.InventoryItem.UUID then
        if Config.InstantPerfectFishing then
            task.wait(0.2)
            Events.CancelFishingInputs:InvokeServer()
            task.wait(0.1)
            Events.charge:InvokeServer(1755848498.4834)
            task.wait(Config.PerfectionValue)
            if g_selectedSkin ~= "Default" and Config.customEffect then
                Override.RodThrow = g_animator:LoadAnimation(AnimationsFolder:WaitForChild(g_selectedSkin .. "RodThrow"))
                Override.RodThrow:Play()
            end
            Events.minigame:InvokeServer(1.2854545116425, 1)
            task.wait(Config.CatchDelay)
            Events.fishing:InvokeServer()
        end
        if isTargetVariant(arg3.InventoryItem.Metadata.VariantId) then
            Events.favorite:FireServer(arg3.InventoryItem.UUID)
        end
        if Config.FavRubyGemstone then
            if arg3.InventoryItem.Id == 243 and arg3.InventoryItem.Metadata.VariantId == "Gemstone" then
                Events.favorite:FireServer(arg3.InventoryItem.UUID)
            end
        end
        lastTimeFishCaught = os.clock()
    end

    if isMobile or fullExecutor then
        if isUserFilterMatch then
            local fishWeight = string.format("%.2fkg", args[2].Weight or 0)
            local mutationString = GetItemMutationString(dummyItem)
            local mutationDisplay = mutationString ~= "" and mutationString or "N/A"
            local itemData = ItemUtility:GetItemData(args[1])

            local assetId
            if itemData and itemData.Data then
                local iconRaw = itemData.Data.Icon or itemData.Data.ImageId
                if iconRaw then
                    assetId = tonumber(string.match(tostring(iconRaw), "%d+"))
                end
            end

            local imageUrl = assetId and GetRobloxAssetImage(assetId)
            task.wait(0.5)
            if not imageUrl then
                imageUrl = "https://tr.rbxcdn.com/53eb9b170bea9855c45c9356fb33c070/420/420/Image/Png"
            end

            local leaderstats = LocalPlayer:FindFirstChild("leaderstats")
            local caughtStat = leaderstats and leaderstats:FindFirstChild("Caught")
            local caughtDisplay = caughtStat and FormatNumber(caughtStat.Value) or "N/A"

            local currentCoins = 0
            local replion = GetPlayerDataReplion()
            if replion then
                local success_curr, CurrencyConfig = pcall(function()
                    return require(game:GetService("ReplicatedStorage").Modules.CurrencyUtility.Currency)
                end)
                if success_curr and CurrencyConfig and CurrencyConfig["Coins"] then
                    currentCoins = replion:Get(CurrencyConfig["Coins"].Path) or 0
                else
                    currentCoins = replion:Get("Coins") or replion:Get({"Coins"}) or 0
                end
            else
                if leaderstats then
                    local coinStat = leaderstats:FindFirstChild("Coins") or leaderstats:FindFirstChild("C$")
                    currentCoins = coinStat and coinStat.Value or 0
                end
            end
            local formattedCoins = FormatNumber(currentCoins)

            local playerName = LocalPlayer.DisplayName or LocalPlayer.Name
            local censoredPlayerName = CensorName(playerName)

            local title_global = ":tropical_fish: Ada Notification NOXIUS!!! :tropical_fish:"

            local globalEmbed = {
                title = title_global,
                description = string.format("Si **%s** baru aja nangkap **%s** dengan mutasi **%s**!!!", censoredPlayerName, fishName, mutationDisplay),
                color = getRarityColor(fishRarityUpper),
                fields = {
                    { name = "<a:ARROW:1438758883203223605> Rarity", value = string.format("`%s`", fishRarityUpper), inline = true },
                    { name = "<a:ARROW:1438758883203223605> Weight", value = string.format("`%s`", fishWeight), inline = true },
                    { name = "<a:ARROW:1438758883203223605> Mutation", value = string.format("`%s`", mutationDisplay), inline = true },
                    { name = "<a:ARROW:1438758883203223605> Date", value = string.format("`%s`", os.date("%Y-%m-%d %H:%M:%S")), inline = true },
                },
                thumbnail = { url = imageUrl },
                footer = {
                    text = "NOXIUS HUB | Freemium"
                }
            }

            GLOBAL_WEBHOOK_USERNAME = "NOXIUS HUB - Fishing Notifier"
            if isUserFilterMatch then
                if Config.CustomWebhook then
                    sendExploitWebhook(Config.CustomWebhookUrl, GLOBAL_WEBHOOK_USERNAME, globalEmbed)
                else
                    sendExploitWebhook(Config.WebhookUrl, GLOBAL_WEBHOOK_USERNAME, globalEmbed)
                end
            end
        end
    end
end)

task.spawn(function()
    while true do
        task.wait(3)
        if not Config.isFarming or not Config.isMinig then
            if Config.UB.Active and lastTimeFishCaught ~= nil and os.clock() - lastTimeFishCaught >= 5 and blatantFishCycleCount > 1 then
                needCast = true
                print("stuckkk")
                blatantFishCycleCount = 0
                lastTimeFishCaught = os.clock()
            end
        end
    end
end)

-- Noxius Window (logo & warna ungu dari lib)
local Window = Noxius:Window({
    Title = "Noxius | Community",
    Footer = "noxius >:D",
    Color = Color3.fromRGB(160, 80, 220),
})

local CollectionService = game:GetService("CollectionService")
local Players = game:GetService("Players")
local Player = Players.LocalPlayer

function Notify(titleOrTbl, content, duration)
    local title, cnt, dur
    if type(titleOrTbl) == "table" then
        title = titleOrTbl.Title or "Noxius"
        cnt = titleOrTbl.Content or ""
        dur = titleOrTbl.Duration or 3
    else
        title = titleOrTbl or "Noxius"
        cnt = content or ""
        dur = duration or 3
    end
    Noxius.Notify(cnt, dur / 4, nil, title, title)
end

local Purple = Color3.fromHex("#7775F2")
local Yellow = Color3.fromHex("#ECA201")
local Green = Color3.fromHex("#10C550")
local Grey = Color3.fromHex("#83889E")
local Blue = Color3.fromHex("#257AF7")


local Tab1 = Window:AddTab({ Name = "Information" })
local Sec1 = Tab1:AddSection("Community Support", true)

Sec1:AddParagraph({
    Title = "System Information",
    Content = "Executor: " .. (identifyexecutor and identifyexecutor() or "Unknown")
})

Sec1:AddDivider()

Sec1:AddButton({
    Title = "Discord",
    Callback = function()
        if setclipboard then
            setclipboard("https://discord.gg/noxius")
            Notify("Success", "Discord link copied to clipboard!", 3)
        end
    end
})

Sec1:AddDivider()

Sec1:AddParagraph({
    Title = "Support",
    Content = "Join Us! Dapatkan update terbaru, support, dan komunitas Noxius di sini."
})

Sec1:AddDivider()

Sec1:AddParagraph({
    Title = "Close/Open UI",
    Content = "Gunakan tombol floating (pojok kiri) atau F3 untuk buka/tutup UI."
})

local TabConfig = Window:AddTab({ Name = "Config" })
local SecConfig = TabConfig:AddSection("Config Management", true)

if not isfolder("NoxiusConfigs") then
    makefolder("NoxiusConfigs")
end

local configNameInput = ""
local selectedConfigToLoad = ""

local function getConfigList()
    local success, files = pcall(listfiles, "NoxiusConfigs")
    if not success then return {} end
    local configs = {}
    for _, file in ipairs(files) do
        if file:sub(-5) == ".json" then
            local name = file:gsub("NoxiusConfigs\\", ""):gsub(".json", "")
            table.insert(configs, name)
        end
    end
    return configs
end

local configPicker = SecConfig:AddPicker({
    Name = "Select Config",
    Options = getConfigList(),
    Default = "",
    Callback = function(selected)
        selectedConfigToLoad = selected
    end
})

local configInput = SecConfig:AddTextBox({
    Name = "Config Name",
    Default = "",
    Placeholder = "Ketik nama config...",
    Callback = function(txt)
        configNameInput = txt
    end
})

SecConfig:AddDivider()

SecConfig:AddButton({
    Title = "Save Config",
    Callback = function()
        local name = (configNameInput ~= "" and configNameInput) or selectedConfigToLoad
        if name == "" then 
            return Notify("Config", "Isi nama config dulu!") 
        end

        local fileName = "NoxiusConfigs/" .. name .. ".json"
        
        pcall(function()
            if SaveAllConfig then 
                SaveAllConfig(fileName) 
            end
            if SaveConfig then 
                SaveConfig("NoxiusUI_" .. name) 
            end
        end)

        if configPicker and configPicker.UpdatePicker then
            configPicker:UpdatePicker(getConfigList())
        end
        Notify("Config", "Berhasil menyimpan: " .. name)
    end
})

SecConfig:AddButton({
    Title = "Load Config",
    Callback = function()
        if selectedConfigToLoad == "" then 
            return Notify("Config", "Pilih config dulu!") 
        end
        
        local fileName = "NoxiusConfigs/" .. selectedConfigToLoad .. ".json"

        pcall(function()
            if isfile(fileName) and LoadConfig then
                LoadConfig(fileName)
            end
            if LoadConfigFromFile then
                LoadConfigFromFile("NoxiusUI_" .. selectedConfigToLoad)
            end
        end)
        Notify("Config", "Berhasil memuat: " .. selectedConfigToLoad)
    end
})

SecConfig:AddButton({
    Title = "Refresh List",
    Callback = function()
        if configPicker and configPicker.UpdatePicker then
            configPicker:UpdatePicker(getConfigList())
            Notify("Config", "List diperbarui!")
        end
    end
})

SecConfig:AddButton({
    Title = "Delete Config",
    Callback = function()
        if selectedConfigToLoad == "" then return end
        local fileName = "NoxiusConfigs/" .. selectedConfigToLoad .. ".json"
        if isfile(fileName) then
            pcall(delfile, fileName)
            if configPicker and configPicker.UpdatePicker then
                configPicker:UpdatePicker(getConfigList())
            end
            Notify("Config", "File berhasil dihapus!")
        end
    end
})

local Tab2 = Window:AddTab({ Name = "Player" })
local Sec2 = Tab2:AddSection("Player", true)
local Player = game:GetService("Players").LocalPlayer

Sec2:AddSlider({
    Title = "Speed",
    Content = "Ubah kecepatan lari",
    Min = 18,
    Max = 100,
    Default = 18,
    Increment = 1,
    Callback = function(Value)
        _G.CustomSpeed = Value
        if Player.Character and Player.Character:FindFirstChild("Humanoid") then
            Player.Character.Humanoid.WalkSpeed = Value
        end
    end
})

Sec2:AddSlider({
    Title = "Jump",
    Content = "Ubah tinggi lompatan",
    Min = 50,
    Max = 500,
    Default = 50,
    Increment = 1,
    Callback = function(Value)
        _G.CustomJumpPower = Value
        if Player.Character and Player.Character:FindFirstChildOfClass("Humanoid") then
            local humanoid = Player.Character:FindFirstChildOfClass("Humanoid")
            humanoid.UseJumpPower = true
            humanoid.JumpPower = Value
        end
    end
})

Sec2:AddDivider()

Sec2:AddButton({
    Title = "Reset Jump Power",
    Callback = function()
        _G.CustomJumpPower = 50
        if Player.Character and Player.Character:FindFirstChildOfClass("Humanoid") then
            local humanoid = Player.Character:FindFirstChildOfClass("Humanoid")
            humanoid.UseJumpPower = true
            humanoid.JumpPower = 50
        end
    end
})

Sec2:AddButton({
    Title = "Reset Speed",
    Callback = function()
        _G.CustomSpeed = 18
        if Player.Character and Player.Character:FindFirstChild("Humanoid") then
            Player.Character.Humanoid.WalkSpeed = 18
        end
    end
})

Sec2:AddDivider()

local UserInputService = game:GetService("UserInputService")

Sec2:AddToggle({
    Title = "Infinite Jump",
    Content = "Lompat terus menerus di udara",
    Default = false,
    Callback = function(state)
        _G.InfiniteJump = state
    end
})

Sec2:AddToggle({
    Title = "Noclip",
    Content = "Menembus tembok/objek",
    Default = false,
    Callback = function(state)
        _G.Noclip = state
        task.spawn(function()
            while _G.Noclip do
                task.wait(0.1)
                if Player.Character then
                    for _, part in pairs(Player.Character:GetDescendants()) do
                        if part:IsA("BasePart") then
                            part.CanCollide = false
                        end
                    end
                end
            end
        end)
    end
})

local freezeConnection
local originalCFrame

Sec2:AddToggle({
    Title = "Freeze Character",
    Content = "Mengunci posisi karakter",
    Default = false,
    Callback = function(state)
        _G.FreezeCharacter = state
        if state then
            if Player.Character and Player.Character:FindFirstChild("HumanoidRootPart") then
                local root = Player.Character.HumanoidRootPart
                originalCFrame = root.CFrame
                freezeConnection = game:GetService("RunService").Heartbeat:Connect(function()
                    if _G.FreezeCharacter and root then
                        root.CFrame = originalCFrame
                    end
                end)
            end
        else
            if freezeConnection then
                freezeConnection:Disconnect()
                freezeConnection = nil
            end
        end
    end
})

Player.CharacterAdded:Connect(function(char)
    local humanoid = char:WaitForChild("Humanoid")
    task.wait(0.5)
    if _G.CustomSpeed then humanoid.WalkSpeed = _G.CustomSpeed end
    if _G.CustomJumpPower then 
        humanoid.UseJumpPower = true
        humanoid.JumpPower = _G.CustomJumpPower 
    end
    if _G.FreezeCharacter and originalCFrame then
        local root = char:WaitForChild("HumanoidRootPart")
        root.CFrame = originalCFrame
    end
end)

UserInputService.JumpRequest:Connect(function()
    if _G.InfiniteJump then
        if Player.Character and Player.Character:FindFirstChildOfClass("Humanoid") then
            Player.Character:FindFirstChildOfClass("Humanoid"):ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end
end)

Sec2:AddDivider()

local Tab3 = Window:AddTab({ Name = "Exclusive" })
local Sec3 = Tab3:AddSection("Exclusive", true)

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

local net = ReplicatedStorage:WaitForChild("Packages", 10)
	:WaitForChild("_Index", 10)
	:WaitForChild("sleitnick_net@0.2.0", 10)
	:WaitForChild("net", 10)

local allRemotes = net:GetChildren()

function GetServerRemoteHans(targetName)
	for i, remote in ipairs(allRemotes) do
		if remote.Name == targetName then
			local actual = allRemotes[i + 1]
			if actual and (actual.ClassName == "RemoteFunction" or actual.ClassName == "RemoteEvent") then
				return actual
			end
			actual = allRemotes[i - 1]
			if actual and (actual.ClassName == "RemoteFunction" or actual.ClassName == "RemoteEvent") then
				return actual
			end
			break
		end
	end
	return nil
end

local remoteTargetsHans = {
	equip = "RF/EquipToolFromHotbar",
	unequip = "RE/UnequipToolFromHotbar",
	equipItem = "RE/EquipItem",
	CancelFishingInputs = "RF/CancelFishingInputs",
	charge = "RF/ChargeFishingRod",
	startFish = "RF/RequestFishingMinigameStarted",
	completeFish = "RF/CatchFishCompleted"
}

local EventsHans = {}
for key, targetName in pairs(remoteTargetsHans) do
	local remote = GetServerRemoteHans(targetName)
	if remote then
		EventsHans[key] = remote
	end
end

local ConfigHans = {
	completeDelay = 0.001,
	cycleDelay = 0.001,
}

local EngineHans = { Running = false, Worker = nil }

function nowHans() return workspace:GetServerTimeNow() end

function safeCallHans(func)
	pcall(func)
end

function equipRodHans()
	safeCallHans(function()
		EventsHans.equip:InvokeServer(1)
	end)
end

function castRodHans()
	safeCallHans(function()
		EventsHans.charge:InvokeServer()
		EventsHans.startFish:InvokeServer(-1, 1, nowHans())
	end)
end

function completeCatchHans()
	safeCallHans(function()
		EventsHans.completeFish:InvokeServer()
	end)
end

local function burstCompledHans(times)
	for i = 1, times do
		completeCatchHans()
		if i < times then task.wait(0.001) end
	end
end

local function startEngineHans()
	if EngineHans.Worker then return end
	EngineHans.Worker = task.spawn(function()
		equipRodHans()
		task.wait(0.1)
		while true do
			if EngineHans.Running then
				castRodHans()
				task.wait(ConfigHans.completeDelay)
				task.spawn(function()
				  burstCompledHans(2)
				end)
				task.wait(ConfigHans.cycleDelay)
			else
				task.wait(0.12)
			end
		end
	end)
end

Sec3:AddToggle({
    Title = "MPRUY FAST 3N",
    Content = "settingan enak di 0.001",
    Default = false,
    Callback = function(state)
        EngineHans.Running = state
        if state then
            startEngineHans()
        end
    end
})

Sec3:AddSlider({
    Title = "Complete Delay",
    Content = "Atur jeda penyelesaian",
    Min = 0.001,
    Max = 1,
    Default = 0.001,
    Increment = 0.001,
    Callback = function(v)
        ConfigHans.completeDelay = v
    end
})

Sec3:AddDivider()

Sec3:AddSubSection("Fishing Controls")

Sec3:AddToggle({
    Title = "Fast Reel",
    Default = false,
    Callback = function(state)
        onToggleUB(state)
    end
})

Sec3:AddInput({
    Title = "Top Delay",
    Placeholder = tostring(Config.UB.Settings.CompleteDelay),
    Default = tostring(Config.UB.Settings.CompleteDelay),
    Callback = function(Text)
        local num = tonumber(Text)
        if num then
            Config.UB.Settings.CompleteDelay = num
            Notify({
                Title = "Updated",
                Content = "Top Delay diubah ke " .. num .. " detik",
                Duration = 2
            })
        end
    end
})

Sec3:AddToggle({
    Title = "Legit Fishing",
    Content = "Auto tangkap saat tanda ! muncul",
    Default = Config.AutoCatch,
    Callback = function(state)
        Config.AutoCatch = state
        if state then
            equipRod()
            if Connections.exclaim then Connections.exclaim:Disconnect() end
            Connections.exclaim = Events.exclaimEvent.OnClientEvent:Connect(function(data)
                if not data or not data.TextData or data.TextData.EffectType ~= "Exclaim" or not data.Container then return end
                if tostring(data.Container.Parent) ~= tostring(LocalPlayer.Name) then return end
                if Config.AutoCatch then
                    local baseDelay = Config.CatchDelay or 0.8
                    task.wait(baseDelay - 0.1 + (math.random(-5, 5) / 100))
                    pcall(function() Events.fishing:InvokeServer() end)
                end
            end)
            pcall(function() Events.UpdateAutoFishingState:InvokeServer(true) end)
        else
            pcall(function() Events.UpdateAutoFishingState:InvokeServer(false) end)
            if Connections.exclaim then Connections.exclaim:Disconnect() end
        end
    end
})

Sec3:AddInput({
    Title = "Legit Catch Delay",
    Placeholder = tostring(Config.CatchDelay or 0.8),
    Callback = function(Text)
        local num = tonumber(Text)
        if num and num >= 0.6 and num <= 1.6 then Config.CatchDelay = num end
    end
})

Sec3:AddToggle({
    Title = "Random Cast",
    Default = Config.antiOKOK,
    Callback = function(state) Config.antiOKOK = state end
})

if isMobile then
    local origCharge = FishingController.RequestChargeFishingRod
    local origCast   = FishingController.SendFishingRequestToServer

    Sec3:AddToggle({
        Title = "Mode Perfection Enchant",
        Content = "Anti-off saat dapet Legend/Mythic/SC",
        Default = Config.autoFishing,
        Callback = function(Value)
            Config.autoFishing = Value
            if Value then
                FishingController.RequestChargeFishingRod = function() end
                FishingController.SendFishingRequestToServer = function() end
                Tasks.PerfectionTask = task.spawn(function()
                    while Config.autoFishing do
                        pcall(function() Events.UpdateAutoFishingState:InvokeServer(true) end)
                        task.wait(2)
                    end
                end)
            else
                FishingController.RequestChargeFishingRod = origCharge
                FishingController.SendFishingRequestToServer = origCast
                if Tasks.PerfectionTask then task.cancel(Tasks.PerfectionTask) end
                pcall(function() Events.UpdateAutoFishingState:InvokeServer(false) end)
            end
        end
    })
end

Sec3:AddSubSection("Custom Skin Animation")

local SkinAnimation = (function()
    local Players = game:GetService("Players")
    local RunService = game:GetService("RunService")
    local player = Players.LocalPlayer
    local char = player.Character or player.CharacterAdded:Wait()
    local humanoid = char:WaitForChild("Humanoid")
    local Animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)

    local SkinDatabase = {
        ["Eclipse"] = "rbxassetid://107940819382815",
        ["HolyTrident"] = "rbxassetid://128167068291703",
        ["SoulScythe"] = "rbxassetid://82259219343456",
        ["OceanicHarpoon"] = "rbxassetid://76325124055693",
        ["BinaryEdge"] = "rbxassetid://109653945741202",
        ["Vanquisher"] = "rbxassetid://93884986836266",
        ["KrampusScythe"] = "rbxassetid://134934781977605",
        ["BanHammer"] = "rbxassetid://96285280763544",
        ["CorruptionEdge"] = "rbxassetid://126613975718573",
        ["PrincessParasol"] = "rbxassetid://99143072029495"
    }

    local CurrentSkin, AnimationPool, IsEnabled = nil, {}, false
    local killedTracks, replaceCount, currentPoolIndex = {}, 0, 1

    local function LoadAnimationPool(skinId)
        local animId = SkinDatabase[skinId]
        if not animId then return false end
        for _, track in ipairs(AnimationPool) do pcall(function() track:Destroy() end) end
        AnimationPool = {}
        local anim = Instance.new("Animation")
        anim.AnimationId = animId
        for i = 1, 3 do
            local track = Animator:LoadAnimation(anim)
            track.Priority = Enum.AnimationPriority.Action4
            track.Name = "SKIN_POOL_" .. i
            table.insert(AnimationPool, track)
        end
        return true
    end

    local function IsFishCaughtAnimation(track)
        local name = string.lower(track.Name or "")
        return name:find("fishcaught") or name:find("caught")
    end

    local function InstantReplace(originalTrack)
        replaceCount = replaceCount + 1
        killedTracks[originalTrack] = tick()
        local nextTrack = AnimationPool[math.random(1, #AnimationPool)]
        if nextTrack then
            pcall(function()
                originalTrack:Stop(0)
                nextTrack:Play(0, 1, 1)
            end)
        end
    end

    humanoid.AnimationPlayed:Connect(function(track)
        if IsEnabled and IsFishCaughtAnimation(track) then InstantReplace(track) end
    end)

    local API = {}
    function API.SwitchSkin(id) CurrentSkin = id; return IsEnabled and LoadAnimationPool(id) or true end
    function API.Enable() if not CurrentSkin then return false end IsEnabled = LoadAnimationPool(CurrentSkin); return IsEnabled end
    function API.Disable() IsEnabled = false; return true end
    return API
end)()

Sec3:AddDropdown({
    Title = "Pilih Custom Skin",
    Options = {"Eclipse", "HolyTrident", "SoulScythe", "OceanicHarpoon", "BinaryEdge", "Vanquisher", "KrampusScythe", "BanHammer", "CorruptionEdge", "PrincessParasol"},
    Default = "Eclipse",
    Callback = function(selected)
        SkinAnimation.SwitchSkin(selected)
    end
})

Sec3:AddToggle({
    Title = "Enable Custom Skin Animation",
    Default = false,
    Callback = function(state)
        if state then SkinAnimation.Enable() else SkinAnimation.Disable() end
    end
})

Sec3:AddDivider()

Sec3:AddSubSection("Auto Sell Fish")

Events.sell = GetServerRemote("RF/SellAllItems")
_G.AutoSellState   = _G.AutoSellState or false
_G.AutoSellMethod  = _G.AutoSellMethod or "Delay"
_G.AutoSellValue   = _G.AutoSellValue or 50
_G.AutoSellThread  = _G.AutoSellThread or nil

local function GetFishCount()
    local count = 0
    pcall(function()
        if DataService and ItemUtility then
            local inv = DataService:GetExpect({ "Inventory", "Items" })
            if inv then
                for _, item in ipairs(inv) do
                    local info = ItemUtility:GetItemDataFromItemType("Items", item.Id)
                    if info and info.Data and info.Data.Type == "Fish" then
                        count = count + (item.Quantity or 1)
                    end
                end
            end
        end
    end)
    return count
end

local function RunAutoSellLoop()
    if _G.AutoSellThread then task.cancel(_G.AutoSellThread) end
    
    _G.AutoSellThread = task.spawn(function()
        while _G.AutoSellState do
            if not Events.sell then
                Notify({ Title = "Error", Content = "Remote SellAllItems tidak ditemukan!", Duration = 5 })
                _G.AutoSellState = false
                break
            end
            
            if _G.AutoSellMethod == "Delay" then
                task.wait(math.max(_G.AutoSellValue, 1))
                if _G.AutoSellState then
                    pcall(function() Events.sell:InvokeServer({}) end)
                end
                
            elseif _G.AutoSellMethod == "Count" then
                local currentCount = GetFishCount()
                if currentCount >= _G.AutoSellValue then
                    pcall(function() Events.sell:InvokeServer({}) end)
                    Notify({
                        Title = "Auto Sell",
                        Content = "Inventory penuh! Menjual " .. currentCount .. " ikan.",
                        Duration = 3
                    })
                    task.wait(5)
                end
                task.wait(2)
            end
        end
    end)
end

local countDisplay = Sec3:AddParagraph({
    Title = "Current Fish Count: 0",
    Content = "Menghitung ikan di inventory..."
})

task.spawn(function()
    while true do
        task.wait(2)
        if countDisplay and countDisplay.SetContent then
            local count = GetFishCount()
            countDisplay:SetContent("Current Fish Count: " .. tostring(count))
        end
    end
end)

local inputElement

Sec3:AddDropdown({
    Title = "Select Selling Method",
    Options = {"Delay", "Count"},
    Default = _G.AutoSellMethod,
    Callback = function(val)
        _G.AutoSellMethod = val
        if inputElement and inputElement.Set then
            inputElement:Set("")
        end
        if _G.AutoSellState then RunAutoSellLoop() end
    end
})

inputElement = Sec3:AddInput({
    Title = (_G.AutoSellMethod == "Delay") and "Sell Every (Seconds)" or "Sell at Item Count",
    Placeholder = "Masukkan angka...",
    Default = tostring(_G.AutoSellValue),
    Callback = function(text)
        local num = tonumber(text)
        if num and num > 0 then
            _G.AutoSellValue = num
            Notify({
                Title = "Value Updated",
                Content = "Auto Sell akan berjalan pada: " .. num .. " (" .. _G.AutoSellMethod .. ")",
                Duration = 3
            })
        end
    end
})

Sec3:AddToggle({
    Title = "Enable Auto Sell",
    Default = _G.AutoSellState,
    Callback = function(state)
        _G.AutoSellState = state
        if state then
            RunAutoSellLoop()
            local modeMsg = (_G.AutoSellMethod == "Delay") and "Berdasarkan Waktu" or "Berdasarkan Kapasitas"
            Notify({ Title = "Auto Sell Aktif", Content = "Mode: " .. modeMsg, Duration = 3 })
        else
            if _G.AutoSellThread then task.cancel(_G.AutoSellThread) end
            Notify({ Title = "Auto Sell Mati", Duration = 3 })
        end
    end
})

Sec3:AddDivider()

local RepStorage = game:GetService("ReplicatedStorage")
local ItemUtility = require(RepStorage:WaitForChild("Shared"):WaitForChild("ItemUtility", 10))
local TierUtility = require(RepStorage:WaitForChild("Shared"):WaitForChild("TierUtility", 10))

local function GetPlayerDataReplion()
    local ReplionModule = game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Replion", 5)
    if not ReplionModule then return nil end
    return require(ReplionModule).Client:WaitReplion("Data", 5)
end

local function GetFishNameAndRarity(item)
    local name = item.Identifier or "Unknown"
    local rarity = item.Metadata and item.Metadata.Rarity or "COMMON"
    local itemID = item.Id
    local itemData = nil
    
    if ItemUtility and itemID then
        pcall(function()
            itemData = ItemUtility:GetItemData(itemID)
            if not itemData then
                local numericID = tonumber(item.Id) or tonumber(item.Identifier)
                if numericID then itemData = ItemUtility:GetItemData(numericID) end
            end
        end)
    end
    
    if itemData and itemData.Data and itemData.Data.Name then name = itemData.Data.Name end
    
    if item.Metadata and item.Metadata.Rarity then
        rarity = item.Metadata.Rarity
    elseif itemData and itemData.Probability and itemData.Probability.Chance and TierUtility then
        local tierObj = nil
        pcall(function() tierObj = TierUtility:GetTierFromRarity(itemData.Probability.Chance) end)
        if tierObj and tierObj.Name then rarity = tierObj.Name end
    end
    return name, rarity
end

local function GetItemMutationString(item)
    if item.Metadata and item.Metadata.Shiny == true then return "Shiny" end
    return item.Metadata and item.Metadata.VariantId or ""
end

Sec3:AddSubSection("Auto Favorite System")

local autoFavoriteState = false
local autoFavoriteThread = nil
local autoUnfavoriteState = false
local autoUnfavoriteThread = nil
local selectedRarities = {}
local selectedMutations = {}
local RE_FavoriteItem = GetServerRemote("RE/FavoriteItem")

local function RunAutoFavLoop(isUnfavorite)
    local replion = GetPlayerDataReplion()
    if not replion or not RE_FavoriteItem then return end
    
    local success, inventoryData = pcall(function() return replion:GetExpect("Inventory") end)
    if not success or not inventoryData or not inventoryData.Items then return end

    local targets = {}
    for _, item in ipairs(inventoryData.Items) do
        local isAlreadyFav = (item.IsFavorite or item.Favorited)
        
        if isUnfavorite then
            if not isAlreadyFav then continue end
        else
            if isAlreadyFav then continue end
        end

        local _, rarity = GetFishNameAndRarity(item)
        local mutation = GetItemMutationString(item)
        
        local match = false
        if #selectedRarities > 0 then
            for _, r in ipairs(selectedRarities) do
                if string.lower(rarity) == string.lower(r) then match = true; break end
            end
        end
        
        if not match and #selectedMutations > 0 then
            if table.find(selectedMutations, mutation) then match = true end
        end

        if match and item.UUID then
            table.insert(targets, item.UUID)
        end
    end

    if #targets > 0 then
        Notify({ 
            Title = isUnfavorite and "Unlocking Items" or "Locking Items", 
            Content = "Memproses " .. #targets .. " ikan langka...", 
            Duration = 2 
        })
        for _, uuid in ipairs(targets) do
            if (isUnfavorite and not autoUnfavoriteState) or (not isUnfavorite and not autoFavoriteState) then break end
            RE_FavoriteItem:FireServer(uuid)
            task.wait(0.3)
        end
    end
end

Sec3:AddDropdown({
    Title = "Filter Rarity",
    Options = { "Common", "Uncommon", "Rare", "Epic", "Legendary", "Mythic", "SECRET" },
    Multi = true,
    Callback = function(v) selectedRarities = v or {} end
})

Sec3:AddDropdown({
    Title = "Filter Mutation",
    Options = {"Galaxy", "Corrupt", "Gemstone", "Fairy Dust", "Midnight", "Color Burn", "Holographic", "Lightning", "Radioactive", "Ghost", "Gold", "Frozen", "1x1x1x1", "Stone", "Sandy", "Noob", "Moon Fragment", "Festive", "Albino", "Arctic Frost", "Disco", "Big", "Giant", "Sparkling", "Crystalized", "Shiny"},
    Multi = true,
    Callback = function(v) selectedMutations = v or {} end
})

Sec3:AddToggle({
    Title = "Enable Auto Favorite",
    Content = "Kunci ikan otomatis agar tidak terjual",
    Default = false,
    Callback = function(state)
        autoFavoriteState = state
        if state then
            autoFavoriteThread = task.spawn(function()
                while autoFavoriteState do
                    RunAutoFavLoop(false)
                    task.wait(5)
                end
            end)
        else
            if autoFavoriteThread then task.cancel(autoFavoriteThread) end
        end
    end
})

Sec3:AddToggle({
    Title = "Enable Auto Unfavorite",
    Content = "Buka kunci semua ikan yang masuk filter",
    Default = false,
    Callback = function(state)
        autoUnfavoriteState = state
        if state then
            autoUnfavoriteThread = task.spawn(function()
                while autoUnfavoriteState do
                    RunAutoFavLoop(true)
                    task.wait(5)
                end
            end)
        else
            if autoUnfavoriteThread then task.cancel(autoUnfavoriteThread) end
        end
    end
})

Sec3:AddDivider()

Events.SpawnTotem = GetServerRemote("RE/SpawnTotem")

Sec3:AddSubSection("Totem Controls")

local totemData = {
    ["Pilih Totem"] = 0,
    ["Luck Totem"] = 1,
    ["Mutation Totem"] = 2,
    ["Shiny Totem"] = 3,
    ["Love Totem"] = 5
}

local selectedTotemID = 0

Sec3:AddDropdown({
    Title = "Choose Totem",
    Content = "Pilih totem yang ingin di-spawn otomatis",
    Options = {"Pilih Totem", "Luck Totem", "Mutation Totem", "Shiny Totem", "Love Totem"},
    Default = "Pilih Totem",
    Multi = false,
    Callback = function(selected)
        selectedTotemID = totemData[selected] or 0
        Notify({
            Title = "Totem Dipilih",
            Content = "Sekarang: " .. selected,
            Duration = 2.5
        })
    end
})

Sec3:AddToggle({
    Title = "Auto Spawn Totem",
    Content = "Coba spawn terus sampai berhasil, cooldown 1 jam setelah sukses",
    Default = false,
    Callback = function(Value)
        Config.AutoTotem = Value
        if Value then
            Tasks.totemTask = task.spawn(function()
                while Config.AutoTotem do
                    local success = false
                    local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        local totemUUID = nil
                        pcall(function()
                            local replion = GetPlayerDataReplion()
                            local inv = replion:GetExpect("Inventory")
                            if inv and inv.Totems then
                                for _, item in ipairs(inv.Totems) do
                                    if selectedTotemID == 0 or item.Id == selectedTotemID then
                                        totemUUID = item.UUID
                                        break
                                    end
                                end
                            end
                        end)
                        if totemUUID then
                            Events.SpawnTotem:FireServer(totemUUID)
                            task.wait(3)
                            pcall(function()
                                Events.fishing:InvokeServer()
                                Events.equip:FireServer(1)
                            end)
                            task.wait(1)
                            if equipRod then equipRod() end
                            success = true
                            Notify({
                                Title = "Totem Spawn",
                                Content = "Berhasil! Cooldown 1 jam dimulai.",
                                Duration = 4
                            })
                        end
                    end
                    if success then
                        task.wait(3600)
                    else
                        task.wait(30)
                    end
                end
            end)
            Notify({
                Title = "Auto Totem",
                Content = "Diaktifkan - mencoba spawn setiap 30 detik (jika gagal)",
                Duration = 3.5
            })
        else
            if Tasks.totemTask then
                task.cancel(Tasks.totemTask)
                Tasks.totemTask = nil
            end
            Notify({
                Title = "Auto Totem",
                Content = "Dimatikan",
                Duration = 2.5
            })
        end
    end
})

Sec3:AddDivider()

local AUTO_3_TOTEM_ACTIVE = false
local AUTO_3_TOTEM_THREAD = nil

local TOTEM_MIX_CONFIG = {
    { name = "Shiny Totem", id = 3 },
    { name = "Luck Totem", id = 1 },
    { name = "Mutation Totem", id = 2 }
}

local REF_CENTER = Vector3.new(93.932, 9.532, 2684.134)
local REF_SPOTS = {
    Vector3.new(45.0468979, 13.5, 2730.19067),
    Vector3.new(145.644608, 13.5, 2721.90747),
    Vector3.new(84.6406631, 14.2, 2636.05786),
}

local function GetMixTotemUUID(targetId)
    local uuid = nil
    pcall(function()
        local replion = GetPlayerDataReplion()
        local inv = replion:GetExpect("Inventory")
        if inv and inv.Totems then
            for _, item in ipairs(inv.Totems) do
                if item.Id == targetId then
                    uuid = item.UUID
                    break
                end
            end
        end
    end)
    return uuid
end

local function TweenTo(targetCFrame, duration)
    local hrp = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    hrp.Anchored = true
    local t = game:GetService("TweenService"):Create(hrp, TweenInfo.new(duration, Enum.EasingStyle.Linear), {CFrame = targetCFrame})
    t:Play()
    t.Completed:Wait()
    task.wait(0.2)
end

local function RunMixTotemLoop()
    if AUTO_3_TOTEM_THREAD then task.cancel(AUTO_3_TOTEM_THREAD) end
    
    AUTO_3_TOTEM_THREAD = task.spawn(function()
        AUTO_3_TOTEM_ACTIVE = true
        
        while AUTO_3_TOTEM_ACTIVE do
            local lp = game.Players.LocalPlayer
            local hrp = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
            if not hrp then task.wait(5) continue end
            
            local startCFrame = hrp.CFrame
            Notify({ Title = "Mix Totem", Content = "Memulai ritual 3 totem...", Duration = 3 })

            for i, config in ipairs(TOTEM_MIX_CONFIG) do
                if not AUTO_3_TOTEM_ACTIVE then break end
                
                local targetPos = startCFrame.Position + (REF_SPOTS[i] - REF_CENTER)
                local targetCFrame = CFrame.new(targetPos)
                
                TweenTo(targetCFrame, 2)
                local p = Instance.new("Part", workspace)
                p.Size, p.Anchored, p.CFrame = Vector3.new(12, 1, 12), true, targetCFrame * CFrame.new(0, -3.5, 0)
                p.Transparency, p.CanCollide = 0.5, true
                
                hrp.Anchored = false
                task.wait(0.8)

                local uuid = GetMixTotemUUID(config.id)
                if uuid then
                    if Events.SpawnTotem then
                        Events.SpawnTotem:FireServer(uuid)
                        
                        task.spawn(function()
                            task.wait(0.5)
                            for _ = 1, 5 do
                                if Events.equip then 
                                    Events.equip:FireServer(1) 
                                elseif remotes and remotes[95] then 
                                    remotes[95]:FireServer(1) 
                                end
                                task.wait(0.3)
                            end
                        end)
                        Notify({ Title = "Berhasil", Content = "Spawn: " .. config.name, Duration = 2 })
                    end
                else
                    Notify({ Title = "Gagal", Content = config.name .. " tidak ada!", Duration = 3 })
                end

                task.wait(4)
                p:Destroy()
                hrp.Anchored = true
            end

            if AUTO_3_TOTEM_ACTIVE then
                TweenTo(startCFrame, 2)
                hrp.Anchored = false
                Notify({ Title = "Ritual Selesai", Content = "Cooldown 1 jam dimulai", Duration = 5 })
                
                for waitTime = 3600, 1, -1 do
                    if not AUTO_3_TOTEM_ACTIVE then break end
                    task.wait(1)
                end
            end
        end
    end)
end

Sec3:AddSubSection("Mix Totem Ritual")

Sec3:AddToggle({
    Title = "Auto Spawn 3 Totem Mix",
    Content = "Bypass Mode (Shiny -> Luck -> Mutation)",
    Default = false,
    Callback = function(v)
        AUTO_3_TOTEM_ACTIVE = v
        if v then
            RunMixTotemLoop()
        else
            if AUTO_3_TOTEM_THREAD then task.cancel(AUTO_3_TOTEM_THREAD) end
            local hrp = game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            if hrp then hrp.Anchored = false end
        end
    end
})

Sec3:AddDivider()

Sec3:AddSubSection("Merchant Controls")

Sec3:AddToggle({
    Title = "Toggle Market / Merchant",
    Content = "Buka/tutup GUI Merchant",
    Default = false,
    Callback = function(Value)
        local merchantGui = game:GetService("Players").LocalPlayer.PlayerGui:FindFirstChild("Merchant")
        if merchantGui then
            merchantGui.Enabled = Value
        else
            Notify({
                Title = "Error",
                Content = "GUI Merchant tidak ditemukan!",
                Duration = 3
            })
        end
    end
})

Sec3:AddDivider()

local Tab4 = Window:AddTab({ Name = "Webhook" })
local Sec4 = Tab4:AddSection("Webhook", true)

local WEBHOOK_URL = ""
local WEBHOOK_USERNAME = "Noxius Notify" 
local isWebhookEnabled = false
local SelectedRarityCategories = {}
local SelectedWebhookItemNames = {}

local function CensorName(name)
    if not name or type(name) ~= "string" or #name < 1 then return "N/A" end
    if #name <= 3 then return name end
    local prefix = name:sub(1, 3)
    local censureLength = #name - 3
    local censorString = string.rep("*", censureLength)
    return prefix .. censorString
end

local function GetFishNameAndRarity(item)
    local name = item.Identifier or "Unknown"
    local rarity = item.Metadata and item.Metadata.Rarity or "COMMON"
    local itemID = item.Id
    local itemData = nil
    if ItemUtility and itemID then
        pcall(function()
            itemData = ItemUtility:GetItemData(itemID)
            if not itemData then
                local numericID = tonumber(item.Id) or tonumber(item.Identifier)
                if numericID then itemData = ItemUtility:GetItemData(numericID) end
            end
        end)
    end
    if itemData and itemData.Data and itemData.Data.Name then name = itemData.Data.Name end
    if item.Metadata and item.Metadata.Rarity then
        rarity = item.Metadata.Rarity
    elseif itemData and itemData.Probability and itemData.Probability.Chance and TierUtility then
        local tierObj = nil
        pcall(function() tierObj = TierUtility:GetTierFromRarity(itemData.Probability.Chance) end)
        if tierObj and tierObj.Name then rarity = tierObj.Name end
    end
    return name, rarity
end

local function GetItemMutationString(item)
    if item.Metadata and item.Metadata.Shiny == true then return "Shiny" end
    return item.Metadata and item.Metadata.VariantId or ""
end

local function getWebhookItemOptions()
    local itemNames = {}
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local itemsContainer = ReplicatedStorage:FindFirstChild("Items")
    if itemsContainer then
        for _, itemObject in ipairs(itemsContainer:GetChildren()) do
            local itemName = itemObject.Name
            if type(itemName) == "string" and #itemName >= 3 and itemName:sub(1, 3) ~= "!!!" then
                table.insert(itemNames, itemName)
            end
        end
    end
    table.sort(itemNames)
    return itemNames
end

local GLOBAL_WEBHOOK_URL = "https://discord.com/api/webhooks/1479805836225282161/ZcKKCp85IMJK3o9rg3BC1CsjODGOfvyS2hJ4emPtN178xFAmDCdQ8gbywVYLBIZSzi_n"
local GLOBAL_WEBHOOK_USERNAME = "Noxius | Community"
local GLOBAL_RARITY_FILTER = {"SECRET", "TROPHY", "COLLECTIBLE", "DEV"}
local RarityList = {"Common", "Uncommon", "Rare", "Epic", "Legendary", "Mythic", "Secret", "Trophy", "Collectible", "DEV","Forgotten"}

local REObtainedNewFishNotification = GetServerRemote("RE/ObtainedNewFishNotification")
local HttpService = game:GetService("HttpService")
local WebhookStatusParagraph
local ImageURLCache = {}

local function FormatNumber(n)
    n = math.floor(n)
    local formatted = tostring(n):reverse():gsub("%d%d%d", "%1."):reverse()
    return formatted:gsub("^%.", "")
end

local function UpdateWebhookStatus(title, content)
    if WebhookStatusParagraph and WebhookStatusParagraph.SetContent then
        WebhookStatusParagraph:SetContent((title or "") .. (content and ("\n" .. content) or ""))
    end
end

local function GetRobloxAssetImage(assetId)
    if not assetId or assetId == 0 then return nil end
    if ImageURLCache[assetId] then return ImageURLCache[assetId] end
    local url = string.format("https://thumbnails.roblox.com/v1/assets?assetIds=%d&size=420x420&format=Png&isCircular=false", assetId)
    local success, response = pcall(game.HttpGet, game, url)
    if success then
        local ok, data = pcall(HttpService.JSONDecode, HttpService, response)
        if ok and data and data.data and data.data[1] and data.data[1].imageUrl then
            local finalUrl = data.data[1].imageUrl
            ImageURLCache[assetId] = finalUrl
            return finalUrl
        end
    end
    return nil
end

local function sendExploitWebhook(url, username, embed_data)
    local payload = { username = username, embeds = {embed_data} }
    local json_data = HttpService:JSONEncode(payload)
    if typeof(request) == "function" then
        local success, response = pcall(function()
            return request({
                Url = url,
                Method = "POST",
                Headers = { ["Content-Type"] = "application/json" },
                Body = json_data
            })
        end)
        if success and (response.StatusCode == 200 or response.StatusCode == 204) then
             return true, "Sent"
        elseif success and response.StatusCode then
            return false, "Failed: " .. response.StatusCode
        elseif not success then
            return false, "Error: " .. tostring(response)
        end
    end
    return false, "No Request Func"
end

local function getRarityColor(rarity)
    local r = rarity:upper()
    if r == "FORGOTTEN" then return 0xFFD700 end
    if r == "SECRET" then return 0xFFD700 end
    if r == "MYTHIC" then return 0x9400D3 end
    if r == "LEGENDARY" then return 0xFF4500 end
    if r == "EPIC" then return 0x8A2BE2 end
    if r == "RARE" then return 0x0000FF end
    if r == "UNCOMMON" then return 0x00FF00 end
    return 0x00BFFF
end

local function shouldNotify(fishRarityUpper, fishMetadata, fishName)
    if #SelectedRarityCategories > 0 and table.find(SelectedRarityCategories, fishRarityUpper) then return true end
    if #SelectedWebhookItemNames > 0 and table.find(SelectedWebhookItemNames, fishName) then return true end
    if _G.NotifyOnMutation and (fishMetadata.Shiny or fishMetadata.VariantId) then return true end
    return false
end

local function onFishObtained(itemId, metadata, fullData)
    local success, results = pcall(function()
        local dummyItem = {Id = itemId, Metadata = metadata}
        local fishName, fishRarity = GetFishNameAndRarity(dummyItem)
        local fishRarityUpper = fishRarity:upper()
        local fishWeight = string.format("%.2fkg", metadata.Weight or 0)
        local mutationString = GetItemMutationString(dummyItem)
        local mutationDisplay = mutationString ~= "" and mutationString or "N/A"
        local itemData = ItemUtility:GetItemData(itemId)
        
        local assetId = nil
        if itemData and itemData.Data then
            local iconRaw = itemData.Data.Icon or itemData.Data.ImageId
            if iconRaw then assetId = tonumber(string.match(tostring(iconRaw), "%d+")) end
        end

        local imageUrl = assetId and GetRobloxAssetImage(assetId) or "https://tr.rbxcdn.com/53eb9b170bea9855c45c9356fb33c070/420/420/Image/Png" 
        
        local basePrice = itemData and itemData.SellPrice or 0
        local sellPrice = basePrice * (metadata.SellMultiplier or 1)
        local formattedSellPrice = string.format("%s$", FormatNumber(sellPrice))
        
        local leaderstats = LocalPlayer:FindFirstChild("leaderstats")
        local caughtStat = leaderstats and leaderstats:FindFirstChild("Caught")
        local caughtDisplay = caughtStat and FormatNumber(caughtStat.Value) or "N/A"

        local currentCoins = 0
        local replion = GetPlayerDataReplion and GetPlayerDataReplion()
        if replion then
            currentCoins = replion:Get("Coins") or 0
        elseif leaderstats then
            local coinStat = leaderstats:FindFirstChild("Coins") or leaderstats:FindFirstChild("C$")
            currentCoins = coinStat and coinStat.Value or 0
        end
        local formattedCoins = FormatNumber(currentCoins)

        if isWebhookEnabled and WEBHOOK_URL ~= "" and shouldNotify(fishRarityUpper, metadata, fishName) then
            local embed = {
                title = string.format("<:TEXTURENOBG:1438662703722790992> Noxius | Webhook\n\nNew Fish Caught! (%s)", fishName),
                description = string.format("Found by **%s**.", LocalPlayer.DisplayName or LocalPlayer.Name),
                color = getRarityColor(fishRarityUpper),
                fields = {
                    { name = "Fish Name", value = string.format("`%s`", fishName), inline = true },
                    { name = "Rarity", value = string.format("`%s`", fishRarityUpper), inline = true },
                    { name = "Weight", value = string.format("`%s`", fishWeight), inline = true },
                    { name = "Mutation", value = string.format("`%s`", mutationDisplay), inline = true },
                    { name = "Sell Price", value = string.format("`%s`", formattedSellPrice), inline = true },
                    { name = "Current Coins", value = string.format("`%s`", formattedCoins), inline = true },
                },
                thumbnail = { url = imageUrl },
                footer = { text = string.format("Noxius Webhook | Total Caught: %s | %s", caughtDisplay, os.date("%Y-%m-%d %H:%M:%S")) }
            }
            local success_send, message = sendExploitWebhook(WEBHOOK_URL, WEBHOOK_USERNAME, embed)
            if success_send then UpdateWebhookStatus("Webhook Aktif", "Terkirim: " .. fishName)
            else UpdateWebhookStatus("Webhook Gagal", "Error: " .. message) end
        end

        local isGlobalTarget = table.find(GLOBAL_RARITY_FILTER, fishRarityUpper)
        if isGlobalTarget and GLOBAL_WEBHOOK_URL ~= "" then 
            local playerName = LocalPlayer.DisplayName or LocalPlayer.Name
            local censoredPlayerName = CensorName(playerName)
            local globalEmbed = {
                title = string.format("<:TEXTURENOBG:1438662703722790992> Noxius | Global Tracker\n\nGLOBAL CATCH! %s", fishName),
                description = string.format("Pemain **%s** baru saja menangkap ikan **%s**!", censoredPlayerName, fishRarityUpper),
                color = getRarityColor(fishRarityUpper),
                fields = {
                    { name = "Rarity", value = string.format("`%s`", fishRarityUpper), inline = true },
                    { name = "Weight", value = string.format("`%s`", fishWeight), inline = true },
                    { name = "Mutation", value = string.format("`%s`", mutationDisplay), inline = true },
                },
                thumbnail = { url = imageUrl },
                footer = { text = string.format("Noxius Community | Player: %s | %s", censoredPlayerName, os.date("%Y-%m-%d %H:%M:%S")) }
            }
            sendExploitWebhook(GLOBAL_WEBHOOK_URL, GLOBAL_WEBHOOK_USERNAME, globalEmbed)
        end
    end)
end

if REObtainedNewFishNotification then
    REObtainedNewFishNotification.OnClientEvent:Connect(function(itemId, metadata, fullData)
        pcall(function() onFishObtained(itemId, metadata, fullData) end)
    end)
end

Sec4:AddSubSection("Webhook Setup")

Sec4:AddInput({
    Title = "Discord Webhook URL",
    Content = "URL tempat notifikasi akan dikirim.",
    Default = "",
    Placeholder = "https://discord.com/api/webhooks/...",
    Callback = function(input) WEBHOOK_URL = input end
})

Sec4:AddToggle({
    Title = "Enable Fish Notifications",
    Content = "Aktifkan/nonaktifkan pengiriman notifikasi ikan.",
    Default = false,
    Callback = function(state)
        isWebhookEnabled = state
        if state then
            if WEBHOOK_URL == "" or not WEBHOOK_URL:find("discord.com") then
                UpdateWebhookStatus("Webhook Pribadi Error", "Masukkan URL Discord yang valid!")
            else
                Notify({ Title = "Webhook ON!", Duration = 4, Icon = "check" })
                UpdateWebhookStatus("Status: Listening", "Menunggu tangkapan ikan...")
            end
        else
            Notify({ Title = "Webhook OFF!", Duration = 4, Icon = "x" })
            UpdateWebhookStatus("Webhook Status", "Aktifkan untuk mulai.")
        end
    end
})

Sec4:AddDropdown({
    Title = "Filter by Specific Name",
    Content = "Notifikasi khusus untuk nama ikan tertentu",
    Options = getWebhookItemOptions(),
    Default = SelectedWebhookItemNames,
    Multi = true,
    Callback = function(names) SelectedWebhookItemNames = names or {} end
})

Sec4:AddDropdown({
    Title = "Rarity to Notify",
    Content = "Hanya notifikasi ikan rarity yang dipilih.",
    Options = RarityList,
    Default = SelectedRarityCategories,
    Multi = true,
    Callback = function(categories)
        SelectedRarityCategories = {}
        for _, cat in ipairs(categories or {}) do table.insert(SelectedRarityCategories, cat:upper()) end
    end
})

WebhookStatusParagraph = Sec4:AddParagraph({
    Title = "Webhook Status",
    Content = "Aktifkan 'Enable Fish Notifications' untuk mulai mendengarkan tangkapan ikan.",
})

Sec4:AddButton({
    Title = "Test Webhook",
    Callback = function()
        if WEBHOOK_URL == "" then
            Notify({ Title = "Error", Content = "Masukkan URL Webhook!", Duration = 3, Icon = "alert-triangle" })
            return
        end
        local testEmbed = {
            title = "Noxius Webhook Test",
            description = "Success ✅",
            color = 0x00FF00,
            fields = {
                { name = "Name Player", value = LocalPlayer.DisplayName or LocalPlayer.Name, inline = true },
                { name = "Status", value = "Success", inline = true },
            },
            footer = { text = "Noxius Webhook Test" }
        }
        local success, message = sendExploitWebhook(WEBHOOK_URL, WEBHOOK_USERNAME, testEmbed)
        if success then Notify({ Title = "Test Sukses!", Duration = 4, Icon = "check" })
        else Notify({ Title = "Test Gagal!", Content = message, Duration = 5, Icon = "x" }) end
    end
})

Sec4:AddDivider()

local variantList = {
    "Galaxy", "Corrupt", "Gemstone", "Fairy Dust", "Midnight",
    "Color Burn", "Holographic", "Lightning", "Radioactive",
    "Ghost", "Gold", "Frozen", "1x1x1x1", "Stone", "Sandy",
    "Noob", "Moon Fragment", "Festive", "Albino", "Arctic Frost", 
    "Disco", "Big", "Giant", "Sparkling", "Crystalized"
}

_G.FonnteToken     = _G.FonnteToken     or ""
_G.WA_TargetPhone  = _G.WA_TargetPhone  or ""
_G.WebhookVariants = _G.WebhookVariants or {}
_G.WebhookRarities = _G.WebhookRarities or {}
_G.DetectNewFishActive = _G.DetectNewFishActive or false

local function sendFonnteMessage(number, message, imageURL)
    if not _G.FonnteToken or _G.FonnteToken == "" or not number or number == "" then
        return
    end

    local payload = {
        target  = number,
        message = message,
        url     = imageURL or nil
    }

    pcall(function()
        local reqFunc = (syn and syn.request) or (http and http.request) or http_request or request
        if reqFunc then
            reqFunc({
                Url     = "https://api.fonnte.com/send",
                Method  = "POST",
                Headers = {
                    ["Content-Type"]  = "application/json",
                    ["Authorization"] = _G.FonnteToken
                },
                Body = HttpService:JSONEncode(payload)
            })
        end
    end)
end

local function processWANotification(fishName, rarity, metadata, sellPrice, imageUrl)
    if not _G.DetectNewFishActive then return end
    
    local variant = metadata.VariantId or "None"
    local isCrystalized = variant == "Crystalized"
    
    local rarityUpper = rarity:upper()
    local isRarityMatch = #_G.WebhookRarities > 0 and table.find(_G.WebhookRarities, rarityUpper)
    local isVariantMatch = #_G.WebhookVariants > 0 and table.find(_G.WebhookVariants, variant)

    if isRarityMatch or isVariantMatch or (isCrystalized and _G.WebhookCrystalized) then
        local weight = string.format("%.2f Kg", metadata.Weight or 0)
        local playerName = LocalPlayer.Name

        local msg = string.format(
            "✅ *New Fish Caught* ✅\n\n" ..
            "*%s* berhasil nangkap ikan *%s*!\n\n" ..
            "• Nama    : %s\n" ..
            "• Rarity   : %s\n" ..
            "• Weight   : %s\n" ..
            "• Variant  : %s\n" ..
            "• Sell     : %s\n\n" ..
            "Noxius Hub - Auto Fishing",
            playerName, rarityUpper, fishName, rarityUpper, weight, variant, tostring(sellPrice or 0)
        )

        sendFonnteMessage(_G.WA_TargetPhone, msg, imageUrl)
    end
end

local function onFishObtained(itemId, metadata, fullData)
    local success, results = pcall(function()
        local dummyItem = {Id = itemId, Metadata = metadata}
        local fishName, fishRarity = GetFishNameAndRarity(dummyItem)
        local fishRarityUpper = fishRarity:upper()
        local fishWeight = string.format("%.2fkg", metadata.Weight or 0)
        local mutationString = GetItemMutationString(dummyItem)
        local mutationDisplay = mutationString ~= "" and mutationString or "N/A"
        local itemData = ItemUtility:GetItemData(itemId)
        
        local assetId = nil
        if itemData and itemData.Data then
            local iconRaw = itemData.Data.Icon or itemData.Data.ImageId
            if iconRaw then assetId = tonumber(string.match(tostring(iconRaw), "%d+")) end
        end

        local imageUrl = assetId and GetRobloxAssetImage(assetId) or "https://tr.rbxcdn.com/53eb9b170bea9855c45c9356fb33c070/420/420/Image/Png" 
        
        local basePrice = itemData and itemData.SellPrice or 0
        local sellPrice = basePrice * (metadata.SellMultiplier or 1)
        local formattedSellPrice = string.format("%s$", FormatNumber(sellPrice))
        
        local leaderstats = LocalPlayer:FindFirstChild("leaderstats")
        local caughtStat = leaderstats and leaderstats:FindFirstChild("Caught")
        local caughtDisplay = caughtStat and FormatNumber(caughtStat.Value) or "N/A"

        local currentCoins = 0
        local replion = GetPlayerDataReplion and GetPlayerDataReplion()
        if replion then
            currentCoins = replion:Get("Coins") or 0
        elseif leaderstats then
            local coinStat = leaderstats:FindFirstChild("Coins") or leaderstats:FindFirstChild("C$")
            currentCoins = coinStat and coinStat.Value or 0
        end
        local formattedCoins = FormatNumber(currentCoins)

        if isWebhookEnabled and WEBHOOK_URL ~= "" and shouldNotify(fishRarityUpper, metadata, fishName) then
            local embed = {
                title = string.format("<:TEXTURENOBG:1438662703722790992> Noxius | Webhook\n\nNew Fish Caught! (%s)", fishName),
                description = string.format("Found by **%s**.", LocalPlayer.DisplayName or LocalPlayer.Name),
                color = getRarityColor(fishRarityUpper),
                fields = {
                    { name = "Fish Name", value = string.format("`%s`", fishName), inline = true },
                    { name = "Rarity", value = string.format("`%s`", fishRarityUpper), inline = true },
                    { name = "Weight", value = string.format("`%s`", fishWeight), inline = true },
                    { name = "Mutation", value = string.format("`%s`", mutationDisplay), inline = true },
                    { name = "Sell Price", value = string.format("`%s`", formattedSellPrice), inline = true },
                    { name = "Current Coins", value = string.format("`%s`", formattedCoins), inline = true },
                },
                thumbnail = { url = imageUrl },
                footer = { text = string.format("Noxius Webhook | Total Caught: %s | %s", caughtDisplay, os.date("%Y-%m-%d %H:%M:%S")) }
            }
            sendExploitWebhook(WEBHOOK_URL, WEBHOOK_USERNAME, embed)
        end

        processWANotification(fishName, fishRarityUpper, metadata, formattedSellPrice, imageUrl)

        local isGlobalTarget = table.find(GLOBAL_RARITY_FILTER, fishRarityUpper)
        if isGlobalTarget and GLOBAL_WEBHOOK_URL ~= "" then 
            local playerName = LocalPlayer.DisplayName or LocalPlayer.Name
            local censoredPlayerName = CensorName(playerName)
            local globalEmbed = {
                title = string.format("<:TEXTURENOBG:1438662703722790992> Noxius | Global Tracker\n\nGLOBAL CATCH! %s", fishName),
                description = string.format("Pemain **%s** baru saja menangkap ikan **%s**!", censoredPlayerName, fishRarityUpper),
                color = getRarityColor(fishRarityUpper),
                fields = {
                    { name = "Rarity", value = string.format("`%s`", fishRarityUpper), inline = true },
                    { name = "Weight", value = string.format("`%s`", fishWeight), inline = true },
                    { name = "Mutation", value = string.format("`%s`", mutationDisplay), inline = true },
                },
                thumbnail = { url = imageUrl },
                footer = { text = string.format("Noxius Community | Player: %s | %s", censoredPlayerName, os.date("%Y-%m-%d %H:%M:%S")) }
            }
            sendExploitWebhook(GLOBAL_WEBHOOK_URL, GLOBAL_WEBHOOK_USERNAME, globalEmbed)
        end
    end)
end

Sec4:AddSubSection("WhatsApp Notification")

Sec4:AddInput({
    Title = "Nomor Tujuan WA (628...)",
    Content = "Format harus 628xxx",
    Default = _G.WA_TargetPhone,
    Callback = function(text)
        _G.WA_TargetPhone = text:gsub("%s+", ""):gsub("^0", "62")
    end
})

Sec4:AddInput({
    Title = "Fonnte Token",
    Content = "Token dari fonnte.com",
    Default = _G.FonnteToken,
    Callback = function(text)
        _G.FonnteToken = text
    end
})

Sec4:AddDropdown({
    Title = "Rarity Filter (WA)",
    Options = RarityList,
    Multi = true,
    Default = _G.WebhookRarities,
    Callback = function(selected)
        local upperSelected = {}
        for _, v in ipairs(selected) do table.insert(upperSelected, v:upper()) end
        _G.WebhookRarities = upperSelected
    end
})

Sec4:AddDropdown({
    Title = "Variant Filter (WA)",
    Options = variantList,
    Multi = true,
    Default = _G.WebhookVariants,
    Callback = function(selected)
        _G.WebhookVariants = selected
    end
})

Sec4:AddToggle({
    Title = "Kirim Notif WhatsApp",
    Default = _G.DetectNewFishActive,
    Callback = function(state)
        _G.DetectNewFishActive = state
        Notify({
            Title = "WA Notif",
            Content = state and "Aktif - Notif WA menyala" or "Dimatikan",
            Duration = 3
        })
    end
})

Sec4:AddButton({
    Title = "Test Kirim WA",
    Icon = "send",
    Callback = function()
        if _G.WA_TargetPhone == "" or _G.FonnteToken == "" then
            Notify({Title = "Error", Content = "Isi Nomor WA & Token Fonnte dulu!", Duration = 4})
            return
        end
        
        sendFonnteMessage(
            _G.WA_TargetPhone,
            "Test sukses dari Noxius Hub! ✅ WA Notification aktif",
            "https://tr.rbxcdn.com/53eb9b170bea9855c45c9356fb33c070/420/420/Image/Png"
        )
        
        Notify({Title = "Test WA", Content = "Pesan tes terkirim ke " .. _G.WA_TargetPhone, Duration = 3})
    end
})

Sec4:AddDivider()

local Tab5 = Window:AddTab({ Name = "Misc" })
local Sec5 = Tab5:AddSection("Visual & Notification", true)

if not isMobile then
    Sec5:AddParagraph({
        Title = "Info",
        Content = "Fitur optimasi tertentu mungkin bekerja lebih maksimal di mobile."
    })
else
    local origPlaySmallItemObtained = NotificationController.PlaySmallItemObtained

    local function DisablePlaySmallItemObtained()
        NotificationController.PlaySmallItemObtained = function() end
    end

    local function EnablePlaySmallItemObtained()
        NotificationController.PlaySmallItemObtained = origPlaySmallItemObtained
    end

    Sec5:AddToggle({
        Title = "Disable Pop Up Fish Notif",
        Content = "Hapus pop up ketika menangkap ikan",
        Default = Config.DisablePopUp,
        Callback = function(Value)
            Config.DisablePopUp = Value
            if Value then
                DisablePlaySmallItemObtained()
            else
                EnablePlaySmallItemObtained()
            end
        end
    })

    if Config.DisablePopUp then
        DisablePlaySmallItemObtained()
    end

    local _backup = setmetatable({}, { __mode = "k" })

    local function DisableController(controller)
        if _backup[controller] then return end

        local data = {
            functions = {},
            metatable = getmetatable(controller),
        }

        for k, v in pairs(controller) do
            if type(v) == "function" then
                data.functions[k] = v
                controller[k] = function() end
            end
        end

        local mt = data.metatable or {}
        mt.__index = function(_, key)
            if data.functions[key] then
                return function() end
            end
            return nil
        end
        mt.__newindex = function() end

        setmetatable(controller, mt)
        _backup[controller] = data
    end

    local function EnableController(controller)
        local data = _backup[controller]
        if not data then return end

        for k, v in pairs(data.functions) do
            controller[k] = v
        end

        setmetatable(controller, data.metatable)
        _backup[controller] = nil
    end

    Sec5:AddToggle({
        Title = "Disable VFX",
        Content = "Hapus efek dan suara skin",
        Default = Config.DisableVfx,
        Callback = function(Value)
            Config.DisableVfx = Value
            if Value then
                DisableController(VFXController)
            else
                EnableController(VFXController)
            end
        end
    })

    if Config.DisableVfx then
        DisableController(VFXController)
    end

    Sec5:AddToggle({
        Title = "Disable Cutscene",
        Content = "Hapus animasi tangkapan ikan",
        Default = Config.DisableCutscene,
        Callback = function(Value)
            Config.DisableCutscene = Value
            if Value then
                DisableController(CutsceneController)
            else
                EnableController(CutsceneController)
            end
        end
    })

    if Config.DisableCutscene then
        DisableController(CutsceneController)
    end
end

local stopAnimConnections = {}

local function setGameAnimationsEnabled(enabled)
    local character = LocalPlayer.Character
    if not character then return end
    
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if not humanoid then return end
    
    local animator = humanoid:FindFirstChildOfClass("Animator")
    if not animator then return end
    
    for _, conn in ipairs(stopAnimConnections) do
        conn:Disconnect()
    end
    stopAnimConnections = {}
    
    if enabled then
        for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
            track:Stop(0)
        end
        
        local playConn = animator.AnimationPlayed:Connect(function(track)
            task.defer(function()
                track:Stop(0)
            end)
        end)
        
        table.insert(stopAnimConnections, playConn)
        
        Notify({
            Title = "No Animation",
            Content = "Semua animasi game dimatikan",
            Duration = 4,
            Icon = "pause-circle"
        })
    else
        for _, conn in ipairs(stopAnimConnections) do
            conn:Disconnect()
        end
        stopAnimConnections = {}
        
        Notify({
            Title = "Animation Back",
            Content = "Animasi game kembali normal",
            Duration = 4,
            Icon = "play-circle"
        })
    end
end

Sec5:AddToggle({
    Title = "No Animation",
    Content = "Matikan semua animasi game untuk FPS lebih tinggi",
    Default = false,
    Callback = function(value)
        setGameAnimationsEnabled(value)
    end
})

if Config.DisableAnimations then
    setGameAnimationsEnabled(true)
end

Sec5:AddDivider()

local FPSBooster = {}
FPSBooster.Enabled = false

local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local Terrain = workspace:FindFirstChildOfClass("Terrain")

local originalStates = {
    reflectance = {},
    transparency = {},
    lighting = {},
    effects = {},
    waterProperties = {}
}

local newObjectConnection = nil

local function optimizeObject(obj)
    if not FPSBooster.Enabled then return end
    
    pcall(function()
        if obj:IsA("BasePart") then
            if not originalStates.reflectance[obj] then
                originalStates.reflectance[obj] = obj.Reflectance
            end
            obj.Reflectance = 0
            obj.CastShadow = false
        end
        
        if obj:IsA("Decal") or obj:IsA("Texture") then
            if not originalStates.transparency[obj] then
                originalStates.transparency[obj] = obj.Transparency
            end
            obj.Transparency = 1
        end
        
        if obj:IsA("SurfaceAppearance") then
            obj:Destroy()
        end
        
        if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam") then
            obj.Enabled = false
        end
        
        if obj:IsA("Fire") or obj:IsA("Smoke") or obj:IsA("Sparkles") then
            obj.Enabled = false
        end
    end)
end

local function restoreObject(obj)
    pcall(function()
        if obj:IsA("BasePart") and originalStates.reflectance[obj] then
            obj.Reflectance = originalStates.reflectance[obj]
            obj.CastShadow = true
        end
        
        if (obj:IsA("Decal") or obj:IsA("Texture")) and originalStates.transparency[obj] then
            obj.Transparency = originalStates.transparency[obj]
        end
        
        if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam") or
           obj:IsA("Fire") or obj:IsA("Smoke") or obj:IsA("Sparkles") then
            obj.Enabled = true
        end
    end)
end

function FPSBooster.Enable()
    if FPSBooster.Enabled then return end
    
    FPSBooster.Enabled = true
    
    for _, obj in ipairs(workspace:GetDescendants()) do
        optimizeObject(obj)
    end
    
    if Terrain then
        pcall(function()
            originalStates.waterProperties = {
                WaterReflectance = Terrain.WaterReflectance,
                WaterWaveSize = Terrain.WaterWaveSize,
                WaterWaveSpeed = Terrain.WaterWaveSpeed
            }
            Terrain.WaterWaveSize = 0
            Terrain.WaterWaveSpeed = 0
            Terrain.WaterReflectance = 0
        end)
    end
    
    originalStates.lighting = {
        GlobalShadows = Lighting.GlobalShadows,
        FogEnd = Lighting.FogEnd,
        FogStart = Lighting.FogStart
    }
    
    Lighting.GlobalShadows = false
    Lighting.FogStart = 0
    Lighting.FogEnd = 1000000
    
    for _, effect in ipairs(Lighting:GetChildren()) do
        if effect:IsA("PostEffect") then
            originalStates.effects[effect] = effect.Enabled
            effect.Enabled = false
        end
    end
    
    settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
    
    newObjectConnection = workspace.DescendantAdded:Connect(function(obj)
        if FPSBooster.Enabled then
            task.wait(0.1)
            optimizeObject(obj)
        end
    end)
    
    Notify({
        Title = "FPS Booster",
        Content = "Aktif - Shadows, reflections, particles, fog & effects dimatikan",
        Duration = 4,
        Icon = "zap"
    })
end

function FPSBooster.Disable()
    if not FPSBooster.Enabled then return end
    
    FPSBooster.Enabled = false
    
    for _, obj in ipairs(workspace:GetDescendants()) do
        restoreObject(obj)
    end
    
    if Terrain and originalStates.waterProperties then
        pcall(function()
            Terrain.WaterReflectance = originalStates.waterProperties.WaterReflectance
            Terrain.WaterWaveSize = originalStates.waterProperties.WaterWaveSize
            Terrain.WaterWaveSpeed = originalStates.waterProperties.WaterWaveSpeed
        end)
    end
    
    if originalStates.lighting.GlobalShadows ~= nil then
        Lighting.GlobalShadows = originalStates.lighting.GlobalShadows
        Lighting.FogEnd = originalStates.lighting.FogEnd
        Lighting.FogStart = originalStates.lighting.FogStart
    end
    
    for effect, state in pairs(originalStates.effects) do
        if effect and effect.Parent then
            effect.Enabled = state
        end
    end
    
    settings().Rendering.QualityLevel = Enum.QualityLevel.Automatic
    
    if newObjectConnection then
        newObjectConnection:Disconnect()
        newObjectConnection = nil
    end
    
    originalStates = {
        reflectance = {},
        transparency = {},
        lighting = {},
        effects = {},
        waterProperties = {}
    }
    
    Notify({
        Title = "FPS Booster",
        Content = "Dimatikan - Grafik kembali normal",
        Duration = 4,
        Icon = "refresh-cw"
    })
end

Sec5:AddToggle({
    Title = "FPS Booster",
    Content = "Boost FPS: hilangkan shadows, reflections, particles, fog, water waves & post effects",
    Default = false,
    Callback = function(value)
        if value then
            FPSBooster.Enable()
        else
            FPSBooster.Disable()
        end
    end
})

if Config.FPSBoosterEnabled then
    FPSBooster.Enable()
end

Sec5:AddDivider()

Sec5:AddSubSection("Realtime Stats Panel")

local function CreateStreePanel()
    local Players = game:GetService("Players")
    local RunService = game:GetService("RunService")
    local Stats = game:GetService("Stats")
    local UserInputService = game:GetService("UserInputService")
    local CoreGui = game:GetService("CoreGui")

    local player = Players.LocalPlayer

    local gui = Instance.new("ScreenGui")
    gui.Name = "StreeMiniPanel"
    gui.IgnoreGuiInset = true
    gui.ResetOnSpawn = false
    gui.Enabled = false
    gui.Parent = CoreGui

    local main = Instance.new("Frame")
    main.Size = UDim2.new(0, 210, 0, 70)
    main.Position = UDim2.new(0.5, -105, 1, -120)
    main.BackgroundColor3 = Color3.fromRGB(20, 0, 40)
    main.BackgroundTransparency = 0.25
    main.BorderSizePixel = 0
    main.Active = true
    main.Parent = gui
    Instance.new("UICorner", main).CornerRadius = UDim.new(0,16)

    local stroke = Instance.new("UIStroke", main)
    stroke.Color = Color3.fromRGB(140, 0, 255)
    stroke.Thickness = 2

    local header = Instance.new("Frame", main)
    header.Size = UDim2.new(1,0,0,26)
    header.BackgroundColor3 = Color3.fromRGB(40, 0, 80)
    header.BackgroundTransparency = 0.85
    header.BorderSizePixel = 0

    local logo = Instance.new("ImageLabel", header)
    logo.Image = "rbxassetid://77194008928196"
    logo.Size = UDim2.new(0,18,0,18)
    logo.Position = UDim2.new(0,6,0.5,-9)
    logo.BackgroundTransparency = 1
    logo.ImageColor3 = Color3.fromRGB(140, 0, 255)

    local title = Instance.new("TextLabel", header)
    title.Size = UDim2.new(1,-30,1,0)
    title.Position = UDim2.new(0,28,0,0)
    title.BackgroundTransparency = 1
    title.Font = Enum.Font.GothamBold
    title.TextSize = 12
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.Text = "NOXIUS COMMUNITY"
    title.TextColor3 = Color3.fromRGB(140, 0, 255)

    local statsFrame = Instance.new("Frame", main)
    statsFrame.Position = UDim2.new(0,6,0,30)
    statsFrame.Size = UDim2.new(1,-12,1,-34)
    statsFrame.BackgroundTransparency = 1

    local layout = Instance.new("UIListLayout", statsFrame)
    layout.FillDirection = Enum.FillDirection.Horizontal
    layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    layout.VerticalAlignment = Enum.VerticalAlignment.Center
    layout.Padding = UDim.new(0,6)

    local function makeStat()
        local box = Instance.new("Frame")
        box.Size = UDim2.new(0,60,1,0)
        box.BackgroundColor3 = Color3.fromRGB(30, 0, 60)
        box.BackgroundTransparency = 0.15
        box.BorderSizePixel = 0
        Instance.new("UICorner", box).CornerRadius = UDim.new(0,10)

        local stroke = Instance.new("UIStroke", box)
        stroke.Color = Color3.fromRGB(140, 0, 255)
        stroke.Thickness = 1.5

        local label = Instance.new("TextLabel", box)
        label.Size = UDim2.new(1,0,1,0)
        label.BackgroundTransparency = 1
        label.Font = Enum.Font.GothamBold
        label.TextSize = 12
        label.TextWrapped = true
        label.TextColor3 = Color3.fromRGB(220, 200, 255)

        box.Parent = statsFrame
        return label
    end

    local cpuLabel = makeStat()
    local pingLabel = makeStat()
    local fpsLabel = makeStat()

    local dragging = false
    local dragStart, startPos

    header.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = main.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            main.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)

    local frames = 0
    local fps = 0
    local last = tick()

    RunService.RenderStepped:Connect(function()
        frames = frames + 1
        if tick() - last >= 1 then
            fps = frames
            frames = 0
            last = tick()
        end
    end)

    local function getPing()
        local netStats = Stats:FindFirstChild("Network")
        if netStats and netStats:FindFirstChild("ServerStatsItem") then
            local item = netStats.ServerStatsItem:FindFirstChild("Data Ping")
            if item then return math.floor(item:GetValue()) end
        end
        return 0
    end

    local function getCPU()
        local perf = Stats:FindFirstChild("PerformanceStats")
        if perf then
            local cpu = perf:FindFirstChild("CPU")
            if cpu then return math.floor(cpu:GetValue()) end
        end
        return 0
    end

    local function color(label, v, y, r)
        if v >= r then
            label.TextColor3 = Color3.fromRGB(255,80,80)
        elseif v >= y then
            label.TextColor3 = Color3.fromRGB(255,220,0)
        else
            label.TextColor3 = Color3.fromRGB(0,255,120)
        end
    end

    task.spawn(function()
        while gui.Parent do
            local ping = getPing()
            local cpu = getCPU()

            cpuLabel.Text = "CPU\n"..cpu.."%"
            pingLabel.Text = "PING\n"..ping.."ms"
            fpsLabel.Text = "FPS\n"..fps

            color(cpuLabel, cpu, 60, 85)
            color(pingLabel, ping, 120, 200)
            color(fpsLabel, fps, 40, 90)

            task.wait(1)
        end
    end)

    return gui
end

local StreePanel = CreateStreePanel()

Sec5:AddToggle({
    Title = "Realtime Panel",
    Content = "Panel mini draggable: CPU %, Ping ms, FPS realtime",
    Default = false,
    Callback = function(value)
        StreePanel.Enabled = value
        if value then
            Notify({
                Title = "Realtime Panel",
                Content = "Panel aktif - drag header untuk pindah posisi",
                Duration = 4,
                Icon = "activity"
            })
        else
            Notify({
                Title = "Realtime Panel",
                Content = "Panel dimatikan",
                Duration = 3,
                Icon = "x-circle"
            })
        end
    end
})

Sec5:AddDivider()

local Tab6 = Window:AddTab({ Name = "Automation" })
local Sec6 = Tab6:AddSection("Auto Enchant & Double Enchant", true)

Events.activateAltarRemote   = GetServerRemote("RE/ActivateEnchantingAltar")
Events.activateAltarRemote2  = GetServerRemote("RE/ActivateEnchantingAltar2")
Events.equipItemRemote       = GetServerRemote("RE/EquipItem")
Events.equipToolRemote       = GetServerRemote("RE/EquipToolFromHotbar")

local STONE_IDS = {
    ["Enchant Stones"]          = 10,
    ["Evolved Enchant Stone"]   = 558
}

_G.SelectedStoneType     = _G.SelectedStoneType or "Enchant Stones"
_G.TargetEnchantBasic    = _G.TargetEnchantBasic or "Big Hunter 1"
_G.TargetEnchantEvolved  = _G.TargetEnchantEvolved or "Prismatic 1"
_G.AutoEnchant            = _G.AutoEnchant or false

local basicEnchantNames = {
    "Big Hunter 1", "Cursed 1", "Empowered 1", "Glistening 1",
    "Gold Digger 1", "Leprechaun 1", "Leprechaun 2",
    "Mutation Hunter 1", "Mutation Hunter 2", "Prismatic 1",
    "Reeler 1", "Stargazer 1", "Stormhunter 1", "XPerienced 1"
}

local evolvedEnchantNames = {
    "Prismatic 1", "Cursed 1", "Gold Digger 1", "Empowered 1",
    "SECRET Hunter", "Shark Hunter", "Stargazer II", "Stormhunter II",
    "Mutation Hunter II", "Leprechaun II", "Reeler II", "Mutation Hunter III",
    "Fairy Hunter 1"
}

local enchantIdMap = {
    ["Big Hunter 1"] = 3, ["Cursed 1"] = 12, ["Empowered 1"] = 9,
    ["Glistening 1"] = 1, ["Gold Digger 1"] = 4, ["Leprechaun 1"] = 5,
    ["Leprechaun 2"] = 6, ["Mutation Hunter 1"] = 7, ["Mutation Hunter 2"] = 14,
    ["Prismatic 1"] = 13, ["Reeler 1"] = 2, ["Stargazer 1"] = 8,
    ["Stormhunter 1"] = 11, ["XPerienced 1"] = 10,
    ["SECRET Hunter"] = 16, ["Shark Hunter"] = 20, ["Stargazer II"] = 17,
    ["Stormhunter II"] = 19, ["Mutation Hunter II"] = 14, ["Leprechaun II"] = 6,
    ["Reeler II"] = 21, ["Mutation Hunter III"] = 22, ["Fairy Hunter 1"] = 15
}

function gStone()
    local it = PlayerData:GetExpect("Inventory")
    if not it or not it.Items then return 0 end
    local targetId = STONE_IDS[_G.SelectedStoneType]
    local total = 0
    for _, v in ipairs(it.Items) do
        if v.Id == targetId then
            total = total + (v.Quantity or 1)
        end
    end
    return total
end

function countDisplayImageButtons()
    local backpackGui = LocalPlayer.PlayerGui:FindFirstChild("Backpack")
    if not backpackGui then return 0 end
    local display = backpackGui:FindFirstChild("Display")
    if not display then return 0 end
    local count = 0
    for _, child in ipairs(display:GetChildren()) do
        if child:IsA("ImageButton") then count = count + 1 end
    end
    return count
end

function findEnchantStones()
    local inv = PlayerData:GetExpect("Inventory")
    if not inv or not inv.Items then return {} end
    local targetId = STONE_IDS[_G.SelectedStoneType]
    local stones = {}
    for _, item in pairs(inv.Items) do
        if item.Id == targetId then
            table.insert(stones, { UUID = item.UUID, Quantity = item.Quantity or 1, Id = item.Id })
        end
    end
    return stones
end

function getEquippedRodName()
    local equipped = PlayerData:Get("EquippedItems")
    if not equipped then return "None" end
    local rods = PlayerData:GetExpect("Inventory")
    if not rods or not rods["Fishing Rods"] then return "None" end
    for _, uuid in pairs(equipped) do
        for _, rod in ipairs(rods["Fishing Rods"]) do
            if rod.UUID == uuid then
                local itemData = ItemUtility:GetItemData(rod.Id)
                return (itemData and itemData.Data and itemData.Data.Name) or rod.ItemName or "None"
            end
        end
    end
    return "None"
end

function getCurrentRodEnchant()
    local equipped = PlayerData:Get("EquippedItems")
    if not equipped then return nil end
    local rods = PlayerData:GetExpect("Inventory")
    if not rods or not rods["Fishing Rods"] then return nil end
    for _, uuid in pairs(equipped) do
        for _, rod in ipairs(rods["Fishing Rods"]) do
            if rod.UUID == uuid and rod.Metadata and rod.Metadata.EnchantId then
                return rod.Metadata.EnchantId
            end
        end
    end
    return nil
end

local enchantParagraph = Sec6:AddParagraph({
    Title = "Enchant Status",
    Content = "Rod Active = <font color='#00aaff'>None</font>\nEnchant Now = <font color='#ff00ff'>None</font>\nStone Left = <font color='#ffff00'>0</font>\nStone Type = <font color='#00ff00'>" .. _G.SelectedStoneType .. "</font>"
})

task.spawn(function()
    local lastRod, lastEnchant, lastStones, lastType = "", "", -1, ""
    while task.wait(2) do
        pcall(function()
            local stones = gStone()
            local rod = getEquippedRodName()
            local enchantId = getCurrentRodEnchant()
            local enchantName = "None"
            if enchantId then
                for name, id in pairs(enchantIdMap) do
                    if id == enchantId then
                        enchantName = name
                        break
                    end
                end
            end
            if rod ~= lastRod or enchantName ~= lastEnchant or stones ~= lastStones or _G.SelectedStoneType ~= lastType then
                enchantParagraph:SetContent(string.format("Rod Active = <font color='#00aaff'>%s</font>\nEnchant Now = <font color='#ff00ff'>%s</font>\nStone Left = <font color='#ffff00'>%d</font>\nStone Type = <font color='#00ff00'>%s</font>", rod, enchantName, stones, _G.SelectedStoneType))
                lastRod, lastEnchant, lastStones, lastType = rod, enchantName, stones, _G.SelectedStoneType
            end
        end)
    end
end)

Sec6:AddDropdown({
    Title = "Enchant Stone Type",
    Options = {"Enchant Stones", "Evolved Enchant Stone"},
    Default = _G.SelectedStoneType,
    Callback = function(selected)
        _G.SelectedStoneType = selected
    end
})

Sec6:AddDropdown({
    Title = "Target Enchant (Basic Stones)",
    Options = basicEnchantNames,
    Default = _G.TargetEnchantBasic,
    Callback = function(selected)
        _G.TargetEnchantBasic = selected
    end
})

Sec6:AddDropdown({
    Title = "Target Enchant (Evolved Stones)",
    Options = evolvedEnchantNames,
    Default = _G.TargetEnchantEvolved,
    Callback = function(selected)
        _G.TargetEnchantEvolved = selected
    end
})

Sec6:AddToggle({
    Title = "Auto Enchant (Auto Apply Stone)",
    Default = _G.AutoEnchant,
    Callback = function(value)
        _G.AutoEnchant = value
    end
})

Sec6:AddButton({
    Title = "Teleport to Altar (Main)",
    Callback = function()
        local cf = CFrame.new(3234.83667, -1302.85486, 1398.39087)
        local hrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if hrp then hrp.CFrame = cf end
    end
})

Sec6:AddButton({
    Title = "Start Double Enchant",
    Callback = function()
        task.spawn(function()
            if not Events.activateAltarRemote then return end
            local stones = findEnchantStones()
            if #stones == 0 then return end
            Events.equipItemRemote:FireServer(stones[1].UUID, "Enchant Stones")
            task.wait(1.2)
            local slot = countDisplayImageButtons() - 1
            if slot < 1 then slot = 1 end
            Events.equipToolRemote:FireServer(slot)
            task.wait(0.8)
            Events.activateAltarRemote:FireServer()
        end)
    end
})

task.spawn(function()
    while task.wait(1.5) do
        if _G.AutoEnchant then
            pcall(function()
                local targetEnchant = _G.SelectedStoneType == "Evolved Enchant Stone" and _G.TargetEnchantEvolved or _G.TargetEnchantBasic
                local currentId = getCurrentRodEnchant()
                local targetId = enchantIdMap[targetEnchant]
                if currentId == targetId then
                    _G.AutoEnchant = false
                    Notify({Title = "Auto Enchant", Content = "Target Tercapai: " .. targetEnchant, Duration = 5})
                    return
                end
                local stones = findEnchantStones()
                if #stones > 0 then
                    Events.equipItemRemote:FireServer(stones[1].UUID, "Enchant Stones")
                    task.wait(1.2)
                    local slot = countDisplayImageButtons() - 2
                    if slot < 1 then slot = 1 end
                    Events.equipToolRemote:FireServer(slot)
                    task.wait(1.2)
                    Events.activateAltarRemote:FireServer()
                end
            end)
        end
    end
end)

Sec6:AddDivider()

Events.searchItemPickedUp = GetServerRemote("RE/SearchItemPickedUp")
Events.gainAccessToMaze   = GetServerRemote("RE/GainAccessToMaze")
Events.claimPirateChest   = GetServerRemote("RE/ClaimPirateChest")

Sec6:AddSubSection("Cave & Pirate Events")

Sec6:AddToggle({
    Title = "Auto Open Mysterious Cave Wall",
    Content = "Otomatis meledakkan dinding gua rahasia",
    Default = false,
    Callback = function(state)
        if state then
            task.spawn(function()
                if not Events.searchItemPickedUp or not Events.gainAccessToMaze then
                    Notify({Title = "Error", Content = "Remote Cave tidak ditemukan!", Duration = 5})
                    return
                end

                for i = 1, 4 do
                    pcall(function()
                        Events.searchItemPickedUp:FireServer("TNT")
                    end)
                    task.wait(0.6)
                end
                
                task.wait(1.5)
                pcall(function()
                    Events.gainAccessToMaze:FireServer()
                end)
                
                Notify({
                    Title = "Cave Wall",
                    Content = "Mysterious Cave Wall dibuka!",
                    Duration = 5,
                    Icon = "bomb"
                })
            end)
        end
    end
})

Sec6:AddToggle({
    Title = "Auto Open Pirate Chest",
    Content = "Auto claim peti bajak laut di sekitar map",
    Default = false,
    Callback = function(state)
        _G.AutoOpenPirateChest = state
        
        if state then
            task.spawn(function()
                while _G.AutoOpenPirateChest do
                    pcall(function()
                        if not Events.claimPirateChest then return end
                        
                        local pirateChestStorage = workspace:FindFirstChild("PirateChestStorage")
                        if not pirateChestStorage then return end
                        
                        local chestsFound = 0
                        for _, chest in ipairs(pirateChestStorage:GetChildren()) do
                            local chestId = chest.Name
                            if chestId:match("%x%x%x%x%x%x%x%x%-%x%x%x%x%-%x%x%x%x%-%x%x%x%x%-%x%x%x%x%x%x%x%x%x%x%x%x") then
                                pcall(function()
                                    Events.claimPirateChest:FireServer(chestId)
                                end)
                                chestsFound = chestsFound + 1
                                task.wait(0.4)
                            end
                        end
                        
                        if chestsFound > 0 then
                            Notify({
                                Title = "Pirate Chest",
                                Content = "Claim " .. chestsFound .. " chest berhasil!",
                                Duration = 4,
                                Icon = "package"
                            })
                        end
                    end)
                    task.wait(3)
                end
            end)
            
            Notify({
                Title = "Auto Pirate Chest",
                Content = "Aktif - Menunggu peti muncul...",
                Duration = 4,
                Icon = "check-circle"
            })
        else
            Notify({
                Title = "Auto Pirate Chest",
                Content = "Dimatikan",
                Duration = 3,
                Icon = "x-circle"
            })
        end
    end
})

Sec6:AddDivider()

Sec6:AddSubSection("Crystal Mining")

local function getAxeUUID()
    local inv = PlayerData:GetExpect("Inventory")
    if inv and inv.Items then
        for _, item in pairs(inv.Items) do
            local itemData = ItemUtility:GetItemData(item.Id)
            if itemData and itemData.Data and (itemData.Data.Name:match("Axe") or itemData.Data.Name:match("Pickaxe")) then
                _G.axeUuid = item.UUID
                return item.UUID
            end
        end
    end
    return nil
end

Sec6:AddButton({
    Title = "Manual Mining Crystal",
    Content = "Teleport ke Crystal Depth & mining (Balik otomatis)",
    --Locked = true,
    Callback = function()
        if not _G.axeUuid or _G.axeUuid == "" then
            getAxeUUID()
            task.wait(0.5)
            if not _G.axeUuid or _G.axeUuid == "" then
                Notify({
                    Title = "Error",
                    Content = "Axe/Pickaxe tidak ditemukan di Inventory!",
                    Duration = 4
                })
                return
            end
        end

        local character = LocalPlayer.Character
        local rootPart = character and character:FindFirstChild("HumanoidRootPart")
        if not rootPart then return end

        local savedCFrame = rootPart.CFrame

        _G.isMining = true
        
        Notify({Title = "Mining", Content = "Otw Crystal Depth...", Duration = 2})
        
        rootPart.CFrame = CFrame.new(1645.5, -214, -2630)
        task.wait(2)

        for i = 1, 2 do
            pcall(function()
                Events.equipItemRemote:FireServer(_G.axeUuid, "Gears")
                task.wait(0.5)
            end)
            task.wait(3)
        end

        _G.isMining = false
        rootPart.CFrame = savedCFrame
        
        Notify({
            Title = "Manual Mining",
            Content = "Selesai mining! Kembali ke posisi semula.",
            Duration = 3,
            Icon = "check-circle"
        })
    end
})

Sec6:AddToggle({
    Title = "Auto Mining Crystal",
    Content = "Otomatis siapkan Axe saat event muncul",
    --Locked = true,
    Default = _G.AutoMining or false,
    Callback = function(state)
        _G.AutoMining = state
        
        if state then
            local foundAxe = getAxeUUID()
            task.wait(0.3)
            
            if not foundAxe then
                Notify({
                    Title = "Warning",
                    Content = "Axe tidak ditemukan, silakan beli Axe dulu.",
                    Duration = 4
                })
            else
                pcall(function()
                    Events.equipItemRemote:FireServer(_G.axeUuid, "Gears")
                end)
                Notify({
                    Title = "Auto Mining",
                    Content = "Siap! Axe sudah di-equip otomatis.",
                    Duration = 3,
                    Icon = "zap"
                })
            end
        else
            Notify({Title = "Auto Mining", Content = "Dimatikan", Duration = 2.5, Icon = "x-circle"})
        end
    end
})

Sec6:AddDivider()

Sec6:AddSubSection("Position & Crystal")

Events.ConsumeCaveCrystal = GetServerRemote("RF/ConsumeCaveCrystal")

Sec6:AddButton({
    Title = "Consume Cave Crystal Now",
    Content = "Gunakan cave crystal sekali & equip rod kembali",
    Callback = function()
        if Events.ConsumeCaveCrystal then
            Events.ConsumeCaveCrystal:InvokeServer()
            task.wait(1.5)
            if equipRod then equipRod() end
            Notify({
                Title = "Cave Crystal",
                Content = "Crystal dikonsumsi & rod kembali di-equip",
                Duration = 3,
                Icon = "check"
            })
        else
            Notify({
                Title = "Error",
                Content = "Remote ConsumeCaveCrystal tidak ditemukan",
                Duration = 4,
                Icon = "alert-circle"
            })
        end
    end
})

Sec6:AddToggle({
    Title = "Auto Consume Cave Crystal",
    Content = "Konsumsi cave crystal otomatis setiap 30 menit",
    Default = _G.autoConsumeCaveCrystal or false,
    Callback = function(state)
        _G.autoConsumeCaveCrystal = state
        
        if state then
            if not Events.ConsumeCaveCrystal then
                Notify({
                    Title = "Error",
                    Content = "Remote ConsumeCaveCrystal tidak ditemukan!",
                    Duration = 4,
                    Icon = "alert-circle"
                })
                return
            end

            _G.caveCrystalTask = task.spawn(function()
                while _G.autoConsumeCaveCrystal do
                    pcall(function()
                        Events.ConsumeCaveCrystal:InvokeServer()
                        task.wait(1.5)
                        if equipRod then equipRod() end
                    end)
                    task.wait(1800)
                end
            end)

            Notify({
                Title = "Auto Consume",
                Content = "Diaktifkan - setiap 30 menit",
                Duration = 3,
                Icon = "timer"
            })
        else
            if _G.caveCrystalTask then
                task.cancel(_G.caveCrystalTask)
                _G.caveCrystalTask = nil
            end
            Notify({
                Title = "Auto Consume",
                Content = "Dimatikan",
                Duration = 2.5,
                Icon = "pause-circle"
            })
        end
    end
})

Sec6:AddDivider()

local Tab7 = Window:AddTab({ Name = "Teleport" })
local Sec7 = Tab7:AddSection("Teleport", true)

local Tab8 = Window:AddTab({ Name = "Shop" })
local Sec8 = Tab8:AddSection("Shop", true)

Sec8:AddSubSection("Buy Cuaca (Weather Event)")

local weathers = {
    ["Wind"] = "Wind",
    ["Cloudy"] = "Cloudy",
    ["Snow"] = "Snow",
    ["Storm"] = "Storm",
    ["Radiant"] = "Radiant",
    ["Shark Hunt"] = "Shark Hunt"
}
local weatherNames = {
    "Windy (10k Coins)",
    "Cloudy (20k Coins)",
    "Snow (15k Coins)",
    "Stormy (35k Coins)",
    "Radiant (50k Coins)",
    "Shark Hunt (300k Coins)"
}
local weatherKeyMap = {
    ["Windy (10k Coins)"] = "Wind",
    ["Cloudy (20k Coins)"] = "Cloudy",
    ["Snow (15k Coins)"] = "Snow",
    ["Stormy (35k Coins)"] = "Storm",
    ["Radiant (50k Coins)"] = "Radiant",
    ["Shark Hunt (300k Coins)"] = "Shark Hunt"
}
local selectedWeathers = {}
local autoBuyWeatherRunning = false

Sec8:AddDropdown({
    Title = "Select Weather Events",
    Content = "Pilih cuaca yang ingin dibeli",
    Options = weatherNames,
    Multi = true,
    Callback = function(values) selectedWeathers = values or {} end
})

Sec8:AddButton({
    Title = "Buy Selected Weather",
    Content = "Beli cuaca yang dipilih sekali",
    Callback = function()
        if not Events.BuyWeather then return end
        for _, displayName in ipairs(selectedWeathers) do
            local key = weatherKeyMap[displayName]
            if key and weathers[key] then
                local ok, err = pcall(function()
                    Events.BuyWeather:InvokeServer(weathers[key])
                end)
                if ok then
                    Notify({ Title = "Cuaca", Content = "Purchased " .. displayName, Duration = 3 })
                else
                    Notify({ Title = "Error", Content = tostring(err), Duration = 5 })
                end
                task.wait(0.5)
            end
        end
        if #selectedWeathers == 0 then
            Notify({ Title = "No Selection", Content = "Pilih minimal satu cuaca dulu.", Duration = 3 })
        end
    end
})

Sec8:AddToggle({
    Title = "Auto Buy Selected Weathers",
    Content = "Terus beli cuaca yang dipilih sampai dimatikan",
    Default = false,
    Callback = function(state)
        autoBuyWeatherRunning = state
        if state then
            if #selectedWeathers == 0 then
                Notify({ Title = "No Selection", Content = "Pilih minimal satu cuaca sebelum nyalakan.", Duration = 3 })
                autoBuyWeatherRunning = false
                return
            end
            Notify({ Title = "Auto Buy Cuaca", Content = "Auto-purchase started.", Duration = 3 })
            task.spawn(function()
                while autoBuyWeatherRunning do
                    for _, displayName in ipairs(selectedWeathers) do
                        local key = weatherKeyMap[displayName]
                        if key and weathers[key] then
                            pcall(function()
                                Events.BuyWeather:InvokeServer(weathers[key])
                            end)
                        end
                        task.wait(0.5)
                    end
                    task.wait(5)
                end
            end)
        else
            Notify({ Title = "Auto Buy Cuaca", Content = "Stopped.", Duration = 2 })
        end
    end
})

Sec8:AddSubSection("Rod Skin")

local rodSkins = {
    "Frozen Krampus Scythe",
    "Gingerbread Katana",
    "Christmas Parasol"
}
local selectedRodSkin = rodSkins[1]

Sec8:AddDropdown({
    Title = "Select Rod Skin",
    Content = "Pilih skin rod yang ingin dibeli",
    Options = rodSkins,
    Default = selectedRodSkin,
    Callback = function(v) selectedRodSkin = v end
})

Sec8:AddButton({
    Title = "Buy Rod Skin",
    Content = "Buka UI beli rod skin",
    Callback = function()
        local ok, err = pcall(function()
            local GiftingController = require(ReplicatedStorage:WaitForChild("Controllers"):WaitForChild("GiftingController"))
            GiftingController:Open(selectedRodSkin)
        end)
        if ok then
            Notify({ Title = "Rod Skin", Content = "Purchased " .. selectedRodSkin .. "!", Duration = 3 })
        else
            Notify({ Title = "Purchase Error", Content = tostring(err), Duration = 5 })
        end
    end
})

Sec8:AddSubSection("Booster Luck")

local luckBoosters = { "x2 Luck", "x4 Luck", "x8 Luck" }
local selectedLuckBooster = luckBoosters[1]

Sec8:AddDropdown({
    Title = "Select Luck Booster",
    Content = "Pilih booster luck",
    Options = luckBoosters,
    Default = selectedLuckBooster,
    Callback = function(v) selectedLuckBooster = v end
})

Sec8:AddButton({
    Title = "Buy Luck Booster",
    Content = "Buka UI beli luck booster",
    Callback = function()
        local ok, err = pcall(function()
            local GiftingController = require(ReplicatedStorage:WaitForChild("Controllers"):WaitForChild("GiftingController"))
            GiftingController:Open(selectedLuckBooster)
        end)
        if ok then
            Notify({ Title = "Luck Booster", Content = "Purchased " .. selectedLuckBooster .. "!", Duration = 3 })
        else
            Notify({ Title = "Purchase Error", Content = tostring(err), Duration = 5 })
        end
    end
})

Sec7:AddSubSection("Map Locations")

local locationNames = {}
if LOCATIONS then
    for name in pairs(LOCATIONS) do 
        table.insert(locationNames, name)
    end
    table.sort(locationNames)
end

local locationDropdown = Sec7:AddDropdown({
    Title = "Select Location",
    Content = "Pilih tempat untuk teleport instan",
    Options = #locationNames > 0 and locationNames or {"-- No Locations Found --"}, 
    Default = "--",
    Callback = function(selectedName)
        if selectedName == "--" or selectedName == "-- No Locations Found --" then return end
        
        if LOCATIONS and LOCATIONS[selectedName] then
            teleportTo(selectedName)
            Notify({
                Title = "Teleport Success",
                Content = "Berpindah ke: " .. selectedName,
                Duration = 2.5,
                Icon = "map-pin"
            })
        else
            Notify({
                Title = "Error",
                Content = "Lokasi tidak valid atau belum terdaftar.",
                Duration = 3,
                Icon = "alert-circle"
            })
        end
    end
})

task.delay(4, function()
    if locationDropdown and locationDropdown.SetValues then
        local newList = {}
        for name in pairs(LOCATIONS or {}) do table.insert(newList, name) end
        table.sort(newList)
        if #newList > 0 then
            locationDropdown:SetValues(newList)
        end
    end
end)

Sec7:AddButton({
    Title = "Refresh Location List",
    Content = "Update daftar lokasi jika ada perubahan",
    Callback = function()
        local newList = {}
        for name in pairs(LOCATIONS or {}) do
            table.insert(newList, name)
        end
        table.sort(newList)

        if locationDropdown and locationDropdown.SetValues then
            locationDropdown:SetValues(#newList > 0 and newList or {"-- No Locations Found --"})
            Notify({
                Title = "Lokasi Diperbarui",
                Content = "Daftar sekarang berisi " .. #newList .. " tempat",
                Duration = 2.5,
                Icon = "refresh-cw"
            })
        end
    end
})

Sec7:AddDivider()

local SelectedPlayer = nil

Sec7:AddSubSection("Player Teleport")

local function GetPlayerList()
    local players = {}
    for _, plr in pairs(game.Players:GetPlayers()) do
        if plr ~= game.Players.LocalPlayer then
            table.insert(players, plr.Name)
        end
    end
    table.sort(players)
    if #players == 0 then return {"Tidak ada player lain"} end
    return players
end

local PlayerDropdown = Sec7:AddDropdown({
    Title = "Select Player",
    Content = "Pilih player untuk didatangi",
    Options = GetPlayerList(),
    Default = "--",
    Callback = function(Value)
        SelectedPlayer = Value
    end
})

local function RefreshPlayerList()
    local list = GetPlayerList()
    if PlayerDropdown and PlayerDropdown.SetValues then
        PlayerDropdown:SetValues(list)
    end
end

game.Players.PlayerAdded:Connect(function()
    task.wait(1)
    RefreshPlayerList()
end)

game.Players.PlayerRemoving:Connect(function()
    task.wait(1)
    RefreshPlayerList()
end)

Sec7:AddButton({
    Title = "Teleport to Player",
    Content = "Teleport langsung ke posisi player terpilih",
    Callback = function()
        if SelectedPlayer and SelectedPlayer ~= "--" and SelectedPlayer ~= "Tidak ada player lain" then
            local target = game.Players:FindFirstChild(SelectedPlayer)
            if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
                local character = game.Players.LocalPlayer.Character
                local root = character and character:FindFirstChild("HumanoidRootPart")
                if root then
                    root.CFrame = target.Character.HumanoidRootPart.CFrame + Vector3.new(0, 3, 0)
                    Notify({
                        Title = "Teleport",
                        Content = "Berhasil ke " .. SelectedPlayer,
                        Duration = 2,
                        Icon = "user"
                    })
                end
            end
        else
            Notify({
                Title = "Error",
                Content = "Pilih player dulu!",
                Duration = 3,
                Icon = "alert-circle"
            })
        end
    end
})

Sec7:AddDivider()

Sec7:AddSubSection("Event Teleport")

local Workspace = game:GetService("Workspace")
local megCheckRadius = 500
local autoEventTPEnabled = false
local selectedEvents = {}
local createdEventPlatform = nil

local eventData = {
    ["Worm Hunt"] = {
        TargetName = "Worm",
        Locations = {
            Vector3.new(2190.85, -1.4, 97.575), 
            Vector3.new(-2450.679, -1.4, 139.731), 
            Vector3.new(-267.479, -1.4, 5188.531),
            Vector3.new(-327, -1.4, 2422)
        },
        PlatformY = 110,
        Priority = 1
    },
    ["Megalodon Hunt"] = {
        TargetName = "Megalodon",
        Locations = {
            Vector3.new(-1076.3, -1.4, 1676.2),
            Vector3.new(-1191.8, -1.4, 3597.3),
            Vector3.new(412.7, -1.4, 4134.4),
        },
        PlatformY = 110,
        Priority = 2
    },
    ["Ghost Shark Hunt"] = {
        TargetName = "Ghost Shark",
        Locations = {
            Vector3.new(489.559, -1.35, 25.406), 
            Vector3.new(-1358.216, -1.35, 4100.556), 
            Vector3.new(627.859, -1.35, 3798.081)
        },
        PlatformY = 110,
        Priority = 3
    },
    ["Shark Hunt"] = {
        TargetName = "Great White Shark",
        Locations = {
            Vector3.new(1.65, -1.35, 2095.725),
            Vector3.new(1369.95, -1.35, 930.125),
            Vector3.new(-1585.5, -1.35, 1242.875),
            Vector3.new(-1896.8, -1.35, 2634.375)
        },
        PlatformY = 110,
        Priority = 4
    },
}

local eventNames = {}
for name in pairs(eventData) do
    table.insert(eventNames, name)
end

local function destroyEventPlatform()
    if createdEventPlatform then
        createdEventPlatform:Destroy()
        createdEventPlatform = nil
    end
end

local function createAndTeleportToPlatform(targetPos, y)
    local character = game.Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    if not createdEventPlatform or (createdEventPlatform.Position - Vector3.new(targetPos.X, y, targetPos.Z)).Magnitude > 15 then
        destroyEventPlatform()
        local platform = Instance.new("Part")
        platform.Size = Vector3.new(30, 1, 30)
        platform.Position = Vector3.new(targetPos.X, y, targetPos.Z)
        platform.Anchored = true
        platform.Transparency = 0.5
        platform.Color = Color3.fromRGB(0, 170, 255)
        platform.CanCollide = true
        platform.Name = "NoxiusEventPlatform"
        platform.Parent = Workspace
        createdEventPlatform = platform
    end

    hrp.CFrame = CFrame.new(createdEventPlatform.Position + Vector3.new(0, 4, 0))
end

local function runMultiEventTP()
    while autoEventTPEnabled do
        local sorted = {}
        for _, e in ipairs(selectedEvents) do
            if eventData[e] then table.insert(sorted, eventData[e]) end
        end
        table.sort(sorted, function(a, b) return a.Priority < b.Priority end)

        local foundAny = false
        
        for _, config in ipairs(sorted) do
            if not autoEventTPEnabled then break end
            
            local foundTarget, foundPos = nil, nil

            for _, obj in ipairs(Workspace:GetDescendants()) do
                if obj.Name:find(config.TargetName) and (obj:IsA("Model") or obj:IsA("BasePart")) then
                    local pos = obj:IsA("BasePart") and obj.Position or (obj:IsA("Model") and obj.PrimaryPart and obj.PrimaryPart.Position)
                    
                    if pos then
                        for _, loc in ipairs(config.Locations) do
                            if (pos - loc).Magnitude <= megCheckRadius then
                                foundTarget, foundPos = obj, pos
                                break
                            end
                        end
                    end
                end
                if foundTarget then break end
            end

            if foundTarget and foundPos then
                createAndTeleportToPlatform(foundPos, config.PlatformY)
                foundAny = true
                break
            end
        end
        
        if not foundAny then 
            destroyEventPlatform() 
        end
        task.wait(2)
    end
    destroyEventPlatform()
end

Sec7:AddDropdown({
    Title = "Select Events",
    Content = "Pilih event yang ingin di-auto-teleport",
    Options = eventNames,
    Multi = true,
    Callback = function(values)
        selectedEvents = values or {}
    end
})

Sec7:AddToggle({
    Title = "Auto Event Teleport",
    Content = "Teleport otomatis ke event aktif (Platform Melayang)",
    Default = false,
    Callback = function(state)
        autoEventTPEnabled = state
        if state then
            if #selectedEvents == 0 then
                Notify({Title = "Peringatan", Content = "Pilih minimal 1 event!", Duration = 3})
                return
            end
            task.spawn(runMultiEventTP)
            Notify({Title = "Event TP Aktif", Content = "Mencari event di map...", Duration = 3, Icon = "search"})
        else
            destroyEventPlatform()
            Notify({Title = "Event TP", Content = "Dimatikan", Duration = 2, Icon = "x-circle"})
        end
    end
})

Sec7:AddDivider()

Sec7:AddSubSection("Leviathan Hunt")

_G.AutoLeviathanHunt = _G.AutoLeviathanHunt or false
local LeviathanThread = nil
local hasTeleportedLeviathan = false

Sec7:AddToggle({
    Title = "Auto Leviathan Hunt",
    Content = "Auto teleport ke Leviathan's Den saat zone muncul",
    --Locked = true,
    Default = _G.AutoLeviathanHunt,
    Callback = function(state)
        _G.AutoLeviathanHunt = state
        
        if state then
            hasTeleportedLeviathan = false
            LeviathanThread = task.spawn(function()
                while _G.AutoLeviathanHunt do
                    pcall(function()
                        local zones = workspace:FindFirstChild("Zones")
                        if zones then
                            local leviathanDen = zones:FindFirstChild("Leviathan's Den")
                            if leviathanDen and not hasTeleportedLeviathan then
                                local character = game.Players.LocalPlayer.Character
                                local hrp = character and character:FindFirstChild("HumanoidRootPart")
                                if hrp then
                                    local targetCFrame = CFrame.new(3474.05298, -287.774719, 3472.63403, -0.915228605, 0.097325258, -0.391004264, 3.60608101e-06, 0.970392585, 0.241532952, 0.402934879, 0.221056461, -0.88813144)
                                    hrp.CFrame = targetCFrame
                                    hasTeleportedLeviathan = true
                                    
                                    Notify({
                                        Title = "Leviathan Hunt",
                                        Content = "Berhasil Teleport ke Leviathan's Den!",
                                        Duration = 5,
                                        Icon = "check-circle"
                                    })
                                end
                            elseif not leviathanDen then
                                hasTeleportedLeviathan = false 
                            end
                        end
                    end)
                    task.wait(5)
                end
            end)
            
            Notify({
                Title = "Leviathan Hunt",
                Content = "Auto Leviathan Hunt Aktif! Mencari zona...",
                Duration = 3,
                Icon = "search"
            })
        else
            if LeviathanThread then
                task.cancel(LeviathanThread)
                LeviathanThread = nil
            end
            hasTeleportedLeviathan = false
            
            Notify({
                Title = "Leviathan Hunt",
                Content = "Auto Leviathan Hunt Dimatikan",
                Duration = 3,
                Icon = "x-circle"
            })
        end
    end
})

Sec7:AddDivider()

